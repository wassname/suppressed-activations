# Job 109 audit

Job 109 added a post-intervention suppressed-token readout to the spider→dog demo from clean commit `8e1c079`; it exited 0. The source readout contains `Spider` and web terms. The independent target readout contains multilingual dog terms. After replacement, the source readout contains `perros`, `hund`, `狗粮`, `Bite`, and `cão`; the answer again changes 8→4. This supports, but does not prove, a semantic dog-component interpretation. Source: [`job_109_full.log`](job_109_full.log), [`../../data/causal_demo.json`](../../data/causal_demo.json).

The strongest alternative is that C=2 directly writes decoder-aligned dog tokens and their associated answer rather than replacing a distinct reasoning variable. The persistent-generation repetition and post-selected strength keep that explanation plausible. The result remains valid as a selected sample-level intervention, not a general causal mechanism.

Written by PI/gpt-5.4.
