# Audit: job 398, default causal demo replication

Job 398 completed successfully with one default condition. The pinned Qwen3.5-4B revision was
loaded once; extraction and generation consumed the same raw prompt tokenization.

Base input is exactly `'Fact: The number of legs on the animal that spins webs is '`. Base has
`p(8)=0.882568`, `p(4)=0.056421`, and its 32-token continuation says the spider has 8 legs.

The default intervention uses the union of suppressed directions detected over the final four
prompt positions, edits the final prompt token at L24 and L26, uses C=2, matches component norms,
and restores each residual norm. It has `p(4)=0.687441`, `p(8)=0.252896`. Its exact continuation
says the unchanged spider subject has 4 legs.

The recomputed readout is:

> `['狗粮', '吠', 'สุนัข', ' собак', ' собаки', ' perros', '养犬', ' cão']`

All eight rows are dog-related; four exactly overlap the donor top eight. Applied
perturbation/residual ratios are 0.429 at L24 and 0.329 at L26. The result therefore cannot be
explained by normalization silently removing the requested dose.

The raw prompt is intentional: it is the exact public input and reproduces the pinned model's
digit-first completion. Separate job 389 documents that the official chat wrapper changes the
base response format to prose-first; job 391 shows that repeated prompt-derived replacement during
chat decoding does not recover the answer change.

Source: [default run](../../out/2026-09-06_213629_causal-demo/run.md) and
[complete condition log](../../out/2026-09-06_213629_causal-demo/conditions/000_default_default/run.md).

-- Codex/gpt-5.6-sol
