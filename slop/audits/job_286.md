# Pueue job 286 audit

Target: `just notebook-run`, run in `/workspace/2026/suppressed-activations` from notebook-recorded source `v0.1.1-71-g3fb504c` on 2026-09-06. Pueue marked the job failed because the post-execution checker rejected a changed Markdown table layout.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| notebook execution | execute all four code cells | Jupytext executed and wrote `nbs/demo.ipynb` | yes | `job_286_full.log:3-7` | none | scientific output exists |
| Base transcript | exact input, readout, 12 tokens, top 10 | all present; first token is 8 | yes | `nbs/demo.ipynb:276-305` | none | requested Base display is usable |
| intervention transcript | same input, replacement readout, 12 tokens, top 10 | all present; first token is 4 | yes | `nbs/demo.ipynb:363-392` | none | requested comparison is usable |
| precision criterion | show six-decimal probabilities | `0.882568`, `0.490091`, and `0.297255` are present | yes | `nbs/demo.ipynb:295,383-384` | none | reviewer finding is resolved |
| post-execution check | accept exactly two top-token tables | assertion expected the old padded header and failed | no | `job_286_full.log:8-17` | none | command exit status is 1 despite valid notebook |
| artifact persistence | retain executed notebook and job evidence | notebook, full log, and pueue status exist | yes | `nbs/demo.ipynb`; `job_286_full.log`; `job_286_status.json` | none | result is auditable |

## Chronology

The job label was:

> why: reviewer required six-decimal probabilities in the comparable transcript; resolve: accept only if clean Qwen notebook shows both exact 12-token generations and p(8)=0.882568, p(4)=0.490091, p(8 after)=0.297255

Pueue started job 286 at `2026-09-06T13:28:33+08:00` and ended it 14.46 seconds later. The complete 17-line log records successful execution first:

> `[jupytext] Executing notebook with kernel python3`
>
> `[jupytext] Writing nbs/demo.ipynb (destination file replaced [use --update to preserve cell outputs and ids])`

The resulting Base output is verbatim:

> `Generation (next 12 tokens, verbatim):`
>
> `8.`
>
> `Hypothesis: The animal that spins webs`
>
> `| 1      | **8**   | -0.125  | 0.882568 |`

The causal-intervention output is also verbatim:

> `Generation (next 12 tokens, verbatim):`
>
> `4.`
>
> `Hypothesis: The animal that spins webs`
>
> `| 1      | **4**   | -0.713  | 0.490091 |`
>
> `| 2      | 8       | -1.213  | 0.297255 |`

The checker then failed on:

> `assert text.count("|   rank | token") == 2`

The notebook now contains `| rank   | token`, because all table values are strings so `tabulate` left-aligns them. Re-running the corrected checker against the same notebook prints:

> `PASS: executed notebook contains one spider-to-dog demo and two top-token tables`

`just check` then passes the projector tests, README check, figure generation, and Python compilation. The saved output is `slop/audits/20260906_transcript_final_check.log`.

## ML-debug form

| row | answer |
|---|---|
| log length and config | 17 complete cleaned lines; notebook output records Qwen3.5-4B revision `851bf6e...`, layers 23/25/32, intervention L26, rank 8, C=4, 12 tokens |
| `SHOULD:` lines | none |
| null or baseline | Base is the unmodified model: p(8)=0.882568. The intervention output is compared directly with it; this run did not recompute random controls because they already live in the cited fixed run. |
| initial demo | Base generated `8.\nHypothesis: The animal that spins webs`. |
| dummy/control | Not part of this presentation-only run; README reports 21/256 prior matched-random interventions equaled or exceeded the effect. |
| held-out comparison | absent; the public text says this is one selected example. |
| schedule | not applicable |
| full sample | both complete input/readout/12-token outputs are quoted above and stored in `nbs/demo.ipynb:276-392` |
| worst step | post-execution string assertion; no optimization, loss, or gradients exist |
| surprise | the notebook met the resolve condition but the recipe failed because `tabulate` changed column alignment when precision was formatted as strings |
| missing evidence | no missing evidence for the presentation check; held-out causal specificity remains outside this job |
| wall-clock and GPU | 14.46 seconds; peak GPU memory was not logged |

## Hypotheses

### H1 [harness | Almost Certain | 99%]

- Mechanism: converting probability cells to six-decimal strings changed `tabulate` alignment, while the checker matched the previous exact header spacing.
- Evidence: the failure quotes `"|   rank | token"`; the notebook contains two `"| rank   | token"` headers and all requested values.
- Contrary evidence: none; the mismatch reproduces deterministically on the saved notebook.
- Discriminating test: count the new header and rerun the checker. It passes.
- Fix/action: update the expected header to the rendered form.
- Interpretability: yes; this does not alter model execution or outputs.

### H2 [bug | Remote | 1%]

- Mechanism: a notebook cell could have failed while Jupytext still wrote a partial artifact.
- Evidence: four code cells have execution counts 1–4 and no error output; both complete displays are present.
- Contrary evidence: the only traceback occurs in the separate checker after notebook writing.
- Discriminating test: inspect every code-cell output and execution count. Completed, with no error output.
- Fix/action: none.
- Interpretability: yes.

### H3 [measurement | Remote | 1%]

- Mechanism: decimal formatting could have changed the underlying model probabilities rather than only their display.
- Evidence: the displayed values exactly match the previously recorded fixed run.
- Contrary evidence: `probability_table` only formats existing `logp`; model execution is unchanged.
- Discriminating test: compare the three six-decimal values with the fixed artifact. They match.
- Fix/action: none.
- Interpretability: yes.

## Decision

1. Resolve-condition verdict: **met**. The clean executed notebook contains both exact 12-token generations and all three required six-decimal probabilities.
2. Prediction check: the sole recorded expectation, preservation of the measured outputs while increasing display precision, is supported.
3. Earliest unsupported link: none in the notebook presentation pipeline. Animal-semantic transfer remains unsupported by the prior scientific controls and is stated as such.
4. Validity: invalid would mean the saved notebook was partial, stale, or numerically different. `P(result is invalid) ≈ 0.01–0.03`; this is a credible presentation result with a failed post-check.
5. Highest-information clues: complete sequential cell counts; exact two generations; exact six-decimal probabilities.
6. Missing metrics: peak GPU memory only. It does not affect the result.
7. Bug requiring code change: replace the obsolete exact table-header string in `scripts/notebook_check.py`.
8. Misconception requiring reinterpretation: pueue `Failed:1` describes the recipe, not the preceding notebook execution.
9. What would change the verdict: an error output, missing cell count, dirty recorded revision, or mismatch from the fixed probabilities.
10. Recommended sequence: keep this executed notebook, correct the checker, run all checks, then commit and push. Do not rerun the model solely to exercise the corrected string assertion.

— PI/gpt-5.4
