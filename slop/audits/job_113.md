# Job 113: executed notebook audit

Target: pueue job 113, `just notebook-run`, run from clean commit `b93720f` on 2026-09-05 17:14:04–17:14:49 +08:00. Complete pueue output: [`job_113_full.log`](job_113_full.log); metadata: [`job_113_status.json`](job_113_status.json).

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| clean execution | every cell executes in order | seven code cells numbered 1–7; no error output | yes | `nbs/demo.ipynb` | environment lock beyond `uv.lock` | notebook state is reproducible through the documented command |
| provenance | notebook names clean source revision | `git: v0.1.1-19-gb93720f` | yes | first visible output | Hugging Face model revision | upstream model changes could alter a future rerun |
| readouts | source, target, and post-replacement lists are visible | spider terms before, dog terms in target, multilingual dog terms after C=2 | yes | visible readout table | readout stability across prompts | the notebook exposes what the detector sees |
| dose sweep | positive and negative strengths show next-token metrics | log odds move from −4.0625 at C=−0.5 to +0.625 at C=2 | yes | visible dose table | held-out strength selection | this is an exploratory curve |
| generations | 64-token text shows normal and excessive intervention | C=0 is coherent; C=2 starts 4 then repeats `吠`; C=−0.5 repeats `丝绸` | yes | visible generation table | none | excessive strengths are visibly malformed |
| figure | Figure 2 appears in the executed notebook | notebook check reports Figure 2 image output | yes | job log | none | public notebook carries the causal summary |
| automated check | notebook check passes | `PASS: executed notebook has clean provenance, both directions, excessive doses, and Figure 2` | yes | job log | assertion for readout table | current check does not explicitly test the new table |

## Evidence and interpretation

The pueue log is 9/9 cleaned lines and ends:

> `PASS: executed notebook has clean provenance, both directions, excessive doses, and Figure 2`

The notebook records the executed configuration:

> `{'git': 'v0.1.1-19-gb93720f', 'model': 'Qwen/Qwen3.5-4B', 'layers': (23, 25, 32), 'intervention': (23, 30), 'rank': 8, 'readout strength': 2.0, 'strengths': (-0.5, -0.25, -0.125, 0.0, 0.125, 0.25, 0.5, 1.0, 2.0)}`

The new visible readout is:

> `spider, before    │ 丝绸, -web, Web, Disc, 的战, Spider, web, WEB`
>
> `dog, before       │ 吠, собаки, 狗粮, dog, Dog, Dog, สุนัข, canine`
>
> `spider, after C=2 │ perros, hund, 狗粮, krém, implanta, cbd, Bite, cão`

The dose table moves `log p(4) − log p(8)` monotonically over positive C: −2.7500 at C=0, −2.6250, −2.5000, −2.1250, −1.3750, and +0.6250 at C=2. This numerical behavior supports a graded local association. It does not establish a general dog representation because the target prompt and C=2 were selected during exploration.

The generation table exposes the failure range. C=0 starts with the expected `8.` and coherent text. C=2 starts `4.` but then repeats `吠`; C=−0.5 repeats `丝绸`. Persistent intervention during decoding writes the fixed target component at every token, so these are expected warnings against treating large C as natural generation.

## ML-debug form

| row | answer |
|---|---|
| log length and config | 9/9 pueue lines; full config is quoted above from the notebook |
| `SHOULD:` lines | none |
| null/baseline | C=0: p8=.8826, p4=.0564, log odds=−2.7500, KL=0 |
| before intervention | coherent 8 continuation and spider/web readout |
| dummy/control | random controls are in Figure 2/data, not recomputed in notebook |
| held-out | none; dog and C=2 were selected during exploration |
| schedule | not applicable |
| full sample | all nine 64-token generations are visible in `nbs/demo.ipynb` |
| worst step | C=2 and C=−0.5 repeat lexical tokens; no gradient or loss exists |
| surprising line | C=1 text says “a dog, which spins webs”; explained: semantic mixing starts before the next-token answer flips |
| missing evidence | fixed model revision, held-out prompts and predeclared strength |
| diagnoses | scientific post-selection 75%; persistent-decoding lexical overwrite 80%; notebook/source mismatch bug 10%; unknown 15% |
| fresh review | reviewer: “The visible outputs are internally consistent with the code and execution history; the main concern is interpretation/generalization” |
| cheapest test | freeze C and prompts, then run a held-out animal-pair set; general behavior predicts target-answer movement without repeated lexical output |
| wall clock and GPU | 44.6 s; peak VRAM not recorded |

A second cause for the monotonic log-odds curve is direct decoder-aligned token writing rather than modification of a hidden reasoning variable. A held-out task metric plus readable generations would separate these interpretations.

## Hypotheses

### H1 [method | Highly Likely | 80%]

- **Mechanism:** Persistent intervention writes a fixed decoder-aligned dog component at every generated token.
- **Evidence:** C=2 generation begins `4.` then repeats `吠`; C=−0.5 repeats `丝绸`.
- **Contrary evidence:** weaker positive strengths remain coherent and move p4 gradually.
- **Discriminating test:** compare prompt-only with persistent generation at the same C; prompt-only should retain the first-token effect without repeated dog tokens.
- **Fix/action:** keep the malformed outputs visible and use prompt-only intervention for the measured figure.
- **Interpretability:** yes for the prompt-only next-token effect; partial for open generation.

### H2 [data | Highly Likely | 75%]

- **Mechanism:** dog and C=2 were selected after inspecting candidates and doses.
- **Evidence:** only dog and cat passed the source scan, and the table shows the answer flips only at C=2.
- **Contrary evidence:** the full dose curve and malformed outputs are retained rather than hidden.
- **Discriminating test:** freeze extraction and C, then evaluate new prompts.
- **Fix/action:** state post-selection beside the notebook result and avoid population claims.
- **Interpretability:** yes as an exploratory demonstration.

### H3 [bug | Remote | 10%]

- **Mechanism:** hidden state could survive between notebook cells and fabricate outputs.
- **Evidence:** notebook formats can permit stale state.
- **Contrary evidence:** execution counts are consecutive 1–7, the command executes from source, and no errors occur.
- **Discriminating test:** rerun `just notebook-run` from a clean commit and compare key outputs.
- **Fix/action:** retain `notebook_check.py` and clean provenance output.
- **Interpretability:** yes; current evidence makes this unlikely.

## Decision

1. **Resolve-condition verdict: met.** The notebook visibly shows all three readouts and `notebook_check` passes.
2. **Prediction check:** clean provenance, readout visibility, and automated pass are supported. No pre-run scientific prediction was recorded.
3. **Earliest unsupported link:** the selected component generalizes beyond this chosen spider/dog pair. A held-out pair evaluation would support it.
4. **Validity:** invalid means stale notebook state or source/output mismatch. `P(invalid) ≈ 0.03–0.10`; credible exploratory demonstration.
5. **Highest-information clues:** consecutive execution counts; exact clean revision; monotonic local log-odds curve plus visible collapse at large magnitude.
6. **Missing metrics:** held-out pairs; pinned model revision; peak VRAM; explicit notebook assertion for the readout table.
7. **Bugs requiring code changes:** no established bug; add an assertion for the readout output if it becomes a stable interface.
8. **Misconceptions requiring reinterpretation:** positive and negative C are two signs of one displacement, not independent dog→spider and spider→dog experiments.
9. **What would change the verdict:** stale or mismatched output would invalidate reproducibility; held-out coherent answer changes would strengthen generality.
10. **Recommended sequence:** add the post-selection sentence, rerun once from a clean commit, then push. Defer new scientific experiments until this public artifact is stable.

Written by PI/gpt-5.4.
