# Job 106 audit

Job 106 intended to regenerate the dog data and figure from clean commit `fb140c0`. It exited 2 before Python ran. `job_106_status.json` records the command as `bash -lc uv run scripts/demo.py && uv run scripts/figure.py`; the intended quoted shell body was not preserved. `bash -lc` therefore invoked `uv` without its `run` arguments, producing the uv help page in [`job_106_full.log`](job_106_full.log).

Hypotheses: (1) harness quoting error, 99%: pueue's stored command lacks the quoted `-lc` argument; use a repository-local shell script. (2) model/runtime error, under 2%: no Python ran. (3) method failure, under 1%: no experiment ran.

Decision: no scientific result exists and the resolve condition is not judgeable. Rerun the same two commands through a script without changing experiment settings.

Written by PI/gpt-5.4.
