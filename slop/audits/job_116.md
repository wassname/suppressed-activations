# Job 116: shell-quoting failure

Target: pueue job 116, intended to execute the Spider→Ant whole-component notebook experiment from commit `7a94f0b`.

| stage | expected | observed | expected? | consequence |
|---|---|---|---|---|
| command parsing | target prompt remains one environment value | pueue stored the prompt without shell quotes | no | `env` tried to execute `The` |
| model execution | load Qwen and run the fixed dose grid | never started | no | no scientific result exists |
| artifact | write `.local/spider_ant_component.ipynb` | absent | no | resolve condition not judgeable |

The complete 1/1-line log says:

> `env: ‘The’: No such file or directory`

Pueue records exit 127 after 0.3 seconds. The executed command stored `SUPPRESSED_TARGET_PROMPT=Fact: The number...`; pueue reparsed the argv and removed the caller's grouping. This reproduces the queue-quoting failure seen in job 106.

## ML-debug form

| row | answer |
|---|---|
| log/config | 1/1 line; full stored command in `job_116_status.json` |
| `SHOULD:` | none |
| baselines and metrics | absent; model inference never began |
| full sample | absent |
| diagnoses | shell-quoting harness bug 99%; missing executable 1%; scientific method 0% from this run |
| cheapest test | put the command and exported environment values in a script, then queue that path |
| wall clock/GPU | 0.3 s; no GPU work |

### H1 [harness | Almost Certain | 99%]
- **Mechanism:** pueue reparsed the prompt's spaces after outer-shell quoting was removed.
- **Evidence:** `env: ‘The’: No such file or directory` and the stored unquoted command.
- **Contrary evidence:** none.
- **Discriminating test:** queue a script containing `export SUPPRESSED_TARGET_PROMPT='...'`; successful model loading confirms it.
- **Fix/action:** same-protocol retry through `.local/run_spider_ant_component.sh`.
- **Interpretability:** no scientific interpretation; yes for command failure.

## Decision

1. **Resolve-condition verdict: not judgeable.** No model code ran.
2. **Prediction check:** all scientific predictions remain unresolved.
3. **Earliest unsupported link:** command parsing; model loading would show it is fixed.
4. **Validity:** `P(scientific result is invalid) ≈ 0.99–1.00`; classification: invalid run.
5. **Highest-information clue:** the only log line identifies argv splitting before Python.
6. **Missing metrics:** every model readout and output.
7. **Bug requiring change:** queue a script instead of a spaced environment value.
8. **Misconception:** exit 127 is not evidence against whole-component replacement.
9. **What changes the verdict:** a same-protocol retry that reaches notebook execution.
10. **Recommended sequence:** retry only the command transport; do not change the experiment.

Written by PI/gpt-5.4.
