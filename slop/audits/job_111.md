# Job 111: Spider→Ant post-swap readout audit

Target: pueue job 111, `uv run scripts/spider_ant_demo.py`, run from commit `552975e515c9bbbf14b414b54f7a6ed4bbbc04be` on 2026-09-05 17:00:15–17:00:56 +08:00.

The job regenerated the existing negative causal test and added a readout after each intervention. The complete cleaned log is [`job_111_full.log`](job_111_full.log); pueue metadata is [`job_111_status.json`](job_111_status.json).

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| clean extraction | detector selects Spider-related content | final rank-8 includes `Spider`, `web`, and `Web`; Spider only at prompt position 13 | yes | `data/spider_ant_demo.json` | none | the readout works on this prompt |
| C=1 coordinate swap | move output toward 6; possibly replace Spider with Ant in readout | log p(6) rises 0.121 nats, but 8 remains top; post-swap rank-8 has neither Spider nor Ant | partial | canonical JSON row | Ant-coordinate magnitude; random token-swap control | no evidence that Ant replaced Spider |
| C=2 extrapolation | a larger semantic move, if the operation is stable | probabilities collapse and generation starts `Spider` | no | job log normalized/all/C=2 row | residual and perturbation norms | C=2 is outside a useful range |
| position control | detected-position-only swap should localize the effect | C=1 gives nearly the same output as all positions | unclear | p6 0.03012 versus 0.03007 | matched null positions | all-position effect is not established as specific |
| generation | answer should change to 6 for a successful causal swap | canonical continuation starts `8.` | no | canonical `generation.text` | clean 64-token generation | the requested Spider→Ant demonstration remains negative |
| persistence | artifact and provenance recorded | JSON records `v0.1.1-18-g552975e`; job exited 0 | yes | metadata and pueue status | package versions and peak VRAM | source revision is recoverable; runtime is less complete |

## Chronological evidence

The script consumed this complete prompt as 14 tokenizer pieces:

> `Fact: The number of legs on the animal that spins webs is `

It made an unmodified first pass, then extracted one rank-8 basis per prompt position at L23/L25/L32 (`scripts/spider_ant_demo.py:45-51`). At the final position, the persisted readout is:

> `丝绸`, `-web`, `Web`, `Disc`, `的战`, `Spider`, `web`, ` WEB`

The script then projected supplied one-token LM-head directions for `Spider` and ` Ant` into each clean basis (`scripts/spider_ant_demo.py:52-57`). It applied a pseudoinverse coordinate exchange through residual layers 23–30 (`scripts/spider_ant_demo.py:22-38`). This is a symmetric coordinate exchange inside a common source-prompt basis; it does not insert a component extracted from an Ant prompt.

For the canonical normalized/all-position/C=1 row, the artifact records:

> `p6: 0.030067918822169304`
>
> `p8: 0.8787134289741516`
>
> `delta_logp6: 0.12062263488769531`
>
> `log_odds_6_vs_8: -3.375`

Clean log odds were −3.500, so the relative shift toward 6 is 0.125 nats. The model still favors 8 by about 29:1. Its generation begins:

> `8.`
> `Hypothesis: The animal that spins webs has 8 legs.`

The new post-swap detector output is:

> `Web`, `Disc`, `web`, `丝绸`, `-web`, `_disc`, `_web`, ` Bomb`

Neither `Spider` nor `Ant` is present. The direct observation is that the transformation removed Spider from this top-8 list while leaving web-related content; interpreting this as an Ant representation would be unsupported.

At C=2, the normalized/all row records `p6=9.17e-35`, `p8=1.05e-33`, and a generation beginning `Spider.` This is an extrapolation failure, not a stronger semantic swap.

## ML-debug form

| row | answer |
|---|---|
| log length and config | 71/71 cleaned lines. Model Qwen3.5-4B; L23/L25/L32 extraction; rank 8; residual L23–L30 intervention; normalized and raw coordinates; all positions and detected position; C∈{1,2}. |
| `SHOULD:` lines | none printed; the run had no predeclared numerical success criterion |
| cited number under a null | clean is the baseline: p6=0.02665, p8=0.88257, log odds=−3.500. No random or null-token coordinate swap was measured. |
| before intervention | clean answer is 8 and clean readout contains `Spider`; this is the base-model result, not chance |
| dummy/control | detected-position-only C=1 is close to all-position C=1, but no content-free matched control exists |
| held-out comparison | none; this is one selected prompt |
| schedule | not applicable |
| one full sample | prompt, all 14 prompt tokens, top-10 next tokens, all eight 64-token continuations, and readouts are in `data/spider_ant_demo.json` |
| worst step | C=2 collapses both numeric probabilities and emits `Spider`; no per-layer norm diagnostics were recorded |
| surprise | `Spider selected at positions: [13]`, yet normalized/all/C=1 post-readout contains neither Spider nor Ant; explained: the coordinate transform changes the detector ranking without implementing an observed Ant-state transfer |
| needed to trust specificity | matched random token coordinates, perturbation norms, Ant-prompt component, and held-out prompts |
| fresh review | reviewer: “the canonical row establishes only that this particular repeated coordinate transformation produces a small, deterministic next-token shift toward `6` while leaving `8` overwhelmingly dominant” |
| cheapest separation test | compare Spider→Ant against norm-matched Spider→random-noun swaps; an Ant-specific mechanism predicts a larger 6-vs-8 shift for Ant |
| wall clock and GPU memory | 41.6 s; peak GPU memory not recorded |

The number at issue, `delta_logp6`, is computed at `scripts/spider_ant_demo.py:81` by subtracting clean log p(6). A generic perturbation could also raise log p(6). A matched random-token distribution would distinguish semantic specificity from generic movement.

## Hypotheses

### H1 [method | Highly Likely | 80%]

- **Mechanism:** Projecting two supplied LM-head rows into the same source-prompt basis and exchanging coordinates does not reconstruct an independently observed Ant state.
- **Evidence:** `scripts/spider_ant_demo.py:54-57` constructs both vectors from LM-head rows, while the post-swap readout is `Web ... Bomb` and contains no Ant.
- **Contrary evidence:** log p(6) rises by 0.121 nats, in the intended direction.
- **Discriminating test:** replace the source component with a component extracted from an Ant prompt; semantic transfer predicts Ant terms and a larger rise in p(6).
- **Fix/action:** keep this as a negative coordinate test; use the already-defined whole-component replacement for a causal positive example.
- **Interpretability:** yes, as a negative result for this implementation.

### H2 [measurement | Likely | 65%]

- **Mechanism:** The 0.121-nat rise in log p(6) is a nonspecific perturbation effect.
- **Evidence:** no random-token coordinate control was run, and C=1 changes detector membership without adding Ant.
- **Contrary evidence:** the direction of change matches the Spider→Ant prediction.
- **Discriminating test:** run 32 or more norm-matched random noun swaps and locate +0.121 nats in that distribution.
- **Fix/action:** do not call the small shift Ant-specific before that control.
- **Interpretability:** partial; the numerical shift is real, its semantic cause is unknown.

### H3 [harness | Likely | 60%]

- **Mechanism:** Repeating the swap at eight layers may overwrite or amplify a coordinate operation that was defined as “full” at C=1.
- **Evidence:** `BLOCKS=range(22,30)` and C=2 causes severe collapse: `p6=9.17e-35`, generation `Spider.`
- **Contrary evidence:** C=1 generations remain coherent.
- **Discriminating test:** apply C=1 at each single layer and compare with one-shot transport across the layer range.
- **Fix/action:** report exact layers and avoid interpreting C as a calibrated semantic dose.
- **Interpretability:** partial.

### H4 [bug | Highly Unlikely | 20%]

- **Mechanism:** Before/after detector readouts could use different indexing or extraction settings.
- **Evidence:** this would fabricate the apparent disappearance of Spider.
- **Contrary evidence:** `scripts/spider_ant_demo.py:47-51` and `70-74` use the same layers, rank, gain, and row normalization; both select the final prompt position.
- **Discriminating test:** assert equality between clean `selected[-1]` and a recomputation through the after-readout code path with no hooks.
- **Fix/action:** add that assertion if this readout becomes a tested API.
- **Interpretability:** yes; this bug is unlikely from code inspection.

## Decision

1. **Resolve-condition verdict: met.** The condition was “persist both readouts and state whether Ant appears after the swap.” Both are persisted; Ant does not appear in the canonical post-swap top 8.
2. **Prediction check:** readouts persist, supported; Ant appearance, no directional prediction was recorded; Spider→Ant answer flip, contradicted by the existing result.
3. **Earliest unsupported link:** a supplied Ant LM-head coordinate becomes an Ant representation. Evidence would be Ant appearing in the readout or an Ant-prompt component producing a selective 6-vs-8 change.
4. **Validity:** invalid means the persisted numbers do not represent the executed code or tensor paths. `P(invalid) ≈ 0.05–0.15`; classify as a credible negative result for this coordinate-swap implementation.
5. **Highest-information clues:** (1) 8 remains favored by 29:1; (2) Ant is absent after the swap; (3) C=2 collapses rather than giving a graded answer change.
6. **Missing metrics:** matched random-token swaps; per-layer intervention effects and norms; independently extracted Ant component; held-out prompt set; peak VRAM.
7. **Bugs requiring code changes:** only H4 merits a cheap no-hook equality assertion; no active bug is established.
8. **Misconceptions requiring reinterpretation:** a symmetric exchange of projected LM-head coordinates is not equivalent to inserting an Ant state; C=2 is not a stronger full swap.
9. **What would change the verdict:** a controlled Ant-prompt component replacement that makes 6 top and exceeds matched semantic and random controls would provide positive causal evidence.
10. **Recommended sequence:** add the before/after readout to the README as negative evidence; keep the working whole-component spider→dog result separate; only then test Ant-prompt component replacement and random noun controls without changing layers or extraction settings.

Written by PI/gpt-5.4.
