# Job 136 audit: import failure

Target: pueue job 136, `uv run scripts/spider_ant_demo.py`, failed before model loading.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| imports | load the root-level method module | `ModuleNotFoundError: No module named 'suppressed_activation_subspace'` | no | complete six-line job log | post-fix command | no experiment ran |
| output initialization | create a unique timestamped directory | not reached | no | traceback occurs at line 15 | output path | no partial run artifact |
| model/evaluation | compare spider and byte prompts | not reached | no | status `Failed: 1` | all scientific metrics | no scientific interpretation |
| persistence | save `log.md`, metadata, result | not reached | no | no new output directory | none | only pueue audit artifacts exist |

## Evidence

The full cleaned log contains 6/6 lines and ends at import:

> `ModuleNotFoundError: No module named 'suppressed_activation_subspace'`

The exact command was reproduced before editing:

> `exit_status=1`

Direct execution places `scripts/` on `sys.path`, while `suppressed_activation_subspace.py` is at the repository root. The current fix restores the earlier two lines before the local import:

> `ROOT = Path(__file__).resolve().parents[1]`
>
> `sys.path.insert(0, str(ROOT))`

Fresh review (`slop/reviews/20260905_job136_crash_review.md`) found no second definite startup blocker and states: “Execution stopped during module import, before `main()`.”

## ML-debug form

| row | answer |
|---|---|
| log/config | 6/6 lines; command and worktree preserved in status JSON. |
| predictions/SHOULD | scientific predictions exist, but no scientific stage ran. |
| null/baseline/init/dummy | absent because import failed. |
| full sample | absent because tokenizer/model loading was not reached. |
| worst step | first local import. |
| surprise | the rewrite removed the old repository-root path insertion; explained and reproduced. |
| missing | post-fix execution. |
| diagnoses | missing root path 99%; packaging/environment drift 5%; another hidden startup problem 10%; unknown 5%. |
| fresh review | exact cause reproduced; current fix assessed as minimal and correct. |
| cheapest test | restart the same pueue job after committing the two-line fix. |
| wall-clock/GPU | about 4 seconds of task runtime; no GPU model load. |

## Hypotheses

### H1 [bug | Almost Certain | 99%]

- Mechanism: direct script execution omitted the repository root from Python’s module search path.
- Evidence: exact import error plus the missing path insertion in the failed revision.
- Contrary evidence: none; the failure reproduces exactly.
- Discriminating test: rerun the same command after restoring the path. It should print the unique output path and load the model.
- Fix/action: add the repository root to `sys.path` before the local import.
- Interpretability: no scientific result; full crash interpretation.

### H2 [harness | Remote | 5%]

- Mechanism: a changed uv environment omitted an installed package.
- Evidence: the command uses `uv run`.
- Contrary evidence: the missing module is a repository file, not a declared package, and its path explains the failure exactly.
- Discriminating test: import after inserting the repository root.
- Fix/action: none unless the rerun still fails.
- Interpretability: no.

### H3 [bug | Remote | 10%]

- Mechanism: another runtime error lies after the fixed import.
- Evidence: the rewritten focused script has not completed once.
- Contrary evidence: py_compile and the library tests pass; fresh static review found no definite blocker.
- Discriminating test: restart the same job.
- Fix/action: audit any new failure rather than adding defensive fallbacks.
- Interpretability: no until rerun.

## Decision

1. Resolve-condition verdict: not judgeable; evaluation never started.
2. Prediction check: all scientific predictions unresolved.
3. Earliest unsupported link: script startup; successful model load is the next evidence.
4. Validity: `P(scientific result is invalid)=1` because none exists. Crash diagnosis is credible.
5. Highest-information clues: import traceback, exact reproduction, local module location.
6. Missing metric: every scientific metric; first rerun the same command.
7. Bug requiring code change: restore repository-root path before local imports.
8. Misconception: none; this is an execution-path regression.
9. What changes the verdict: the same command reaching model load and writing its unique run directory.
10. Recommended sequence: commit the fix, restart job 136, then audit the completed result. Do not change experiment settings.

Written by PI/gpt-5.4.
