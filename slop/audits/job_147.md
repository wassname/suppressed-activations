# Audit: pueue job 147

Target: one-layer paired sample-component sweep, pueue job 147. It ran `uv run scripts/spider_ant_demo.py` at clean source `v0.1.1-54-geee340a` and failed with exit 1 after 32.2 seconds.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| provenance | fixed Ant/Dog targets and grid | clean source; L26, rank16, C=1,2,4,8,12 | yes | partial metadata | model revision | intended code is recoverable |
| model load | load Qwen3.5-4B | 426/426 tensors loaded | yes | full log | peak memory | failure is after load |
| sample extraction | derive independent target subspaces | execution reached first Ant dose | unclear | traceback path | selected rows | values were not persisted |
| intervention | apply at final token only | float mask passed to `torch.where` | no | exception | all scores | no experimental result |
| persistence | complete run artifact | placeholder metadata/log remain `running`; no result JSON | no | partial run directory | failure status in artifact | queue status and this audit are required context |

## Chronology and evidence

The queue label required both distinct crossings:

> `resolve: one-layer whole sample-component mode is viable only if fixed C grid gives both Ant-6 and Dog-4 distinct crossings`

Source: `slop/audits/job_147_status.json`. The full clean log is 66/66 lines. Model loading completed before the first sample-component dose failed:

> `patched = torch.where(mask[None, :, None], candidate, h)`
>
> `RuntimeError: where expected condition to be a boolean tensor, but got a tensor with dtype Float`

Source: `slop/audits/job_147_full.log`. The executed source constructed `mask = torch.zeros(...)` without a dtype. Existing paths use the mask arithmetically; only the new hook uses it as a `torch.where` condition.

A minimal tensor reproduction gives the same exception and exit 1 in `slop/audits/job_147_reproduction.log`. The correct fix is to construct the position mask as boolean. This changes no intended 0/1 multiplication behavior in earlier paths.

No probability, generation, or target readout was persisted. The artifact contains only startup metadata and a placeholder log, both marked `running`.

## ml-debug form

| row | answer |
|---|---|
| log length/config | 66/66 lines; Qwen3.5-4B, L26, rank16, fixed Ant/Dog C grid. |
| SHOULD lines | none; preregistered distinct crossings remain untested. |
| null/scale | no scientific number produced. Exit 1 versus required 0. |
| initial demo | not persisted. |
| dummy/control | not reached. |
| baseline/held-out | not reached. |
| schedule | not applicable. |
| full sample | prompt in metadata; no output. |
| worst step | first sample-component hook crashes before scoring. |
| surprise | old paths accepted the float mask because multiplication casts it. Explained by a new boolean-condition use. |
| missing | every Ant/Dog metric, generation, and control. |
| diagnoses | float-mask bug 99%; next runtime bug 20%; unknown 5%. |
| fresh review | “A floating-point mask is passed as the condition to `torch.where`.” See `slop/reviews/20260905_job147_crash_review.md`. |
| cheapest separator | boolean mask plus one hook-level tensor test. |
| time/memory | 32.2 seconds; peak memory not persisted. The hook test runs on CPU. |

## Hypotheses

### H1 [bug | Almost Certain | 99%]

- Mechanism: a float position mask is invalid as `torch.where`'s condition.
- Evidence: the exact exception appears in both the full model run and minimal reproduction.
- Contrary evidence: none.
- Discriminating test: construct a boolean mask and execute `sample_component_hook` on small tensors.
- Fix/action: `torch.zeros(..., dtype=torch.bool)` and `mask[-1] = True`.
- Interpretability: software diagnosis yes; scientific result no.

### H2 [harness | Highly Unlikely | 20%]

- Mechanism: another new sample-component code path may fail after this first error.
- Evidence: this path has never completed.
- Contrary evidence: compilation and the existing functional test pass.
- Discriminating test: hook-level test, then unchanged full rerun.
- Fix/action: audit the rerun rather than adding fallbacks.
- Interpretability: no for job 147.

### H3 [harness | Almost Certain | 95%]

- Mechanism: exceptions leave partial artifacts incorrectly marked `running`.
- Evidence: queue status is failed, while partial metadata and log say running and no result JSON exists.
- Contrary evidence: pueue itself records failure correctly.
- Discriminating test: inspect any failed run directory.
- Fix/action: later persist terminal failure status and exception while re-raising; do not mix this with the scientific rerun.
- Interpretability: partial when paired with queue status and audit.

## Decision

1. Resolve-condition verdict: **not judgeable**; no Ant or Dog score was persisted.
2. Prediction check: all preregistered scientific possibilities are **unresolved**.
3. Earliest unsupported link: the first sample-component intervention did not execute. A completed first dose is required.
4. Validity: invalid means the run cannot answer its queue-label question. `P(result is invalid) ≈ 0.99`; classification: **invalid run**, not a scientific negative.
5. Highest-information clues: exact exception; minimal reproduction; absence of result JSON.
6. Missing metrics: every target curve, clean target output, selected rows, generations, and controls.
7. Bugs requiring code changes: H1 boolean mask; H3 failure-state persistence later.
8. Misconceptions requiring reinterpretation: none; no result exists.
9. What changes the verdict: an unchanged rerun completing both target curves.
10. Recommended sequence: run the hook-level test, commit the minimal mask fix and crash audit, then restart the same experiment without changing any scientific setting.

Written by PI/gpt-5.4.
