# Job 335 audit

Target: pueue job 335, `just intervention-sweep`, commit `3f32fba` with only the user's README edit dirty. Full 21-line cleaned log and raw log read on 2026-09-06. Job ran 19:03:15–19:03:25 +08:00 and exited 1.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| prompt construction | chat-template prompt containing the exact trailing-space content | template trimmed the space; exact subsequence had zero matches | no | `ValueError: expected one content span, found 0` | rendered prompt was not persisted before failure | no extraction result |
| extraction | persistent source and donor readouts | not reached | no | traceback ends in `chat_input_ids` | all readout scores | no scientific interpretation |
| causal sweep | 1,152 conditions | not reached | no | failure precedes `extract` | all probabilities | no causal interpretation |
| artifact | unique `run.md` and `result.json` | output directory exists without results | no | recipe exited 1 | complete demo | resolve condition not met |

## Chronology

The job loaded all 426 weight shards, then failed during source prompt construction:

> `content_start = find_subsequence(input_ids[0].tolist(), content_ids)`
>
> `ValueError: expected one content span, found 0`

The pinned Qwen template applies Jinja `|trim` to every message. Direct inspection after the failure showed that content `'abc '` renders as content ending in `'abc<|im_end|>'`. Thus the separately tokenized content, which includes the trailing-space token, cannot occur inside the templated IDs. No model forward, readout, metric, generation, or result artifact was produced.

## Hypotheses

### H1 [harness | Almost Certain | 99%]

- **Mechanism:** Qwen's template removes the required trailing space before tokenization.
- **Evidence:** The full log says `expected one content span, found 0`; direct pinned-template inspection contains `render_content(message.content, true)|trim` and renders `'abc '` without its space.
- **Contrary evidence:** None measured; the behavior was reproduced directly.
- **Discriminating test:** Render with the official template, insert the space at the user-content boundary, tokenize, and assert the exact content ID sequence occurs once.
- **Fix/action:** Preserve the template-generated wrapper but restore the source string before `<|im_end|>`.
- **Interpretability:** no; only the harness failure is interpretable.

### H2 [method | Remote | 5%]

- **Mechanism:** Even after prompt repair, strict minimum aggregation may still select multilingual noise, as job 328 did.
- **Evidence:** Job 328's source persistent readout began `[' indywidual', ' الاسلام', 'ปา']`.
- **Contrary evidence:** Job 328 omitted the trailing-space position used by the earlier semantic readout, so it is not the corrected test.
- **Discriminating test:** Run the identical extraction with `spins / webs / is / space` and inspect all four score columns.
- **Fix/action:** Only if it remains noise, compare union/rank aggregation without changing prompt or layers.
- **Interpretability:** partial for job 328; not testable in job 335.

### H3 [bug | Remote | 2%]

- **Mechanism:** More than template trimming caused the missing subsequence.
- **Evidence:** A missing subsequence can in principle arise from boundary-dependent tokenization.
- **Contrary evidence:** The rendered string visibly omits the space and the template explicitly trims it.
- **Discriminating test:** Assert equality at both rendered-string and token-ID levels after boundary restoration.
- **Fix/action:** Keep both assertions fail-fast.
- **Interpretability:** no.

## Decision

1. **Resolve-condition verdict:** not met. The requested “semantic persistent tokens and full causal demo in unique `run.md`” were not produced.
2. **Prediction check:** The trailing-space correction was expected to reach extraction; contradicted. Semantic improvement remains unresolved.
3. **Earliest unsupported link:** exact prompt construction. Support requires the complete source IDs to occur exactly once inside the final templated IDs.
4. **Validity:** Invalid means any claimed readout or causal result from this job. `P(result is invalid)=99%`; classification: invalid, because no result exists.
5. **Highest-information clues:** (1) zero subsequence matches localizes failure before inference; (2) template source explicitly uses `|trim`; (3) direct render removes the space.
6. **Missing metrics:** persistent token scores; base next-token distribution; causal grid; complete generations.
7. **Bugs requiring code changes:** H1 → restore the space after template rendering and assert exact IDs; H3 → retain string and ID assertions.
8. **Misconceptions requiring reinterpretation:** Calling `apply_chat_template` does not imply byte-preservation of message content; this template trims it deliberately.
9. **What would change the verdict:** A successful rerun with exact prompt provenance changes only the new job's verdict, not job 335's.
10. **Recommended sequence:** repair and unit-test prompt construction; rerun the same readout configuration; inspect it before changing aggregation or normalization.

-- Codex/gpt-5.6-sol
