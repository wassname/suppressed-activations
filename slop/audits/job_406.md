# Audit: job 406, executed assistant-prefill notebook

Job 406 executed the pinned Qwen3.5-4B notebook at L24 over the final four fact tokens with
C=2.5. Base generated 8 with `p(8)=0.945299` and `p(4)=0.028546`. The causal condition generated
4 with `p(4)=0.701417` and `p(8)=0.200959`. Its recomputed readout contains dog-related tokens,
while both thirty-two-token continuations remain grammatical and identify the animal as a spider.

The job command exited nonzero only because `scripts/notebook_check.py` still expected the older
raw-prompt continuation. Inspection showed complete notebook outputs and no notebook error. The
checker was updated to the verbatim assistant-prefill continuation and measured probabilities;
`uv run scripts/notebook_check.py` and `just check` then passed.

Source: [executed notebook](../../nbs/demo.ipynb). The queue record is pueue job 406.

-- Codex/gpt-5.6-sol
