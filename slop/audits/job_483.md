# Audit: job 483, ant held-out launch failure

Job 483 did not load the model or produce scientific evidence. Its full seven-line log ends with:

```text
oat_sweep.py: error: unrecognized arguments: The number of legs on the animal that lives in colonies and follows pheromone trails is
```

Pueue reparsed the space-containing `--target-prompt` value. The retry uses the named `ant`
preset so that the queued command has no space-containing argument. The scientific configuration
is unchanged.

Source: pueue job 483 raw log at `/home/code/.local/share/pueue/task_logs/483.log`.

-- Codex/gpt-5.6-sol
