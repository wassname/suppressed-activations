# Pueue job 324 audit

Target: `just notebook-run` in `/workspace/2026/suppressed-activations`; the notebook records clean source `v0.1.1-73-g7985960` and pinned Qwen3.5-4B revision `851bf6e806efd8d0a36b00ddf55e13ccb7b8cd0a`.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| execution | four ordered cells, no errors | counts 1–4, no error outputs | yes | `nbs/demo.ipynb` | peak GPU memory | notebook is executable |
| Base | exact input, post-hoc readout, 32 tokens, top 10 | complete; generation starts `8.` | yes | `nbs/demo.ipynb:278-313` | none | Base is judgeable |
| intervention | same input, recomputed readout, 32 tokens, top 10 | complete; generation starts `4.` | yes | `nbs/demo.ipynb:372-409` | none | conditions are directly comparable |
| Δ log p | causal table relative to Base | `4: +2.162`; `8: -1.088` | yes | `nbs/demo.ipynb:397-408` | none | probability movement is explicit |
| emphasis | expected bold, alternative italic | Base: `**8**`, `*4*`; causal: `**4**`, `*8*` | yes | `nbs/demo.ipynb:303-304,399-400` | none | intended comparison is visible |
| checks | notebook checker passes | `PASS: executed notebook contains one spider-to-dog demo and two top-token tables` | yes | `job_324_full.log:8-9` | none | resolve condition met |

## Chronology

The job label was:

> why: public demo now requires the actual post-intervention readout, delta log probabilities, and exact 32-token continuations; resolve: accept only if the clean Qwen notebook reproduces the README values and both complete displays

The complete nine-line log records notebook execution, writing, and a successful checker. Pueue ran it from `2026-09-06T15:52:29+08:00` to `15:52:50+08:00` and recorded `Success`.

The Base output is:

> `Readout ("what it is thinking but not saying"):`
>
> `['丝绸', '-web', 'Web', 'Disc', '的战', 'Spider', 'web', ' WEB']`
>
> `8.`
>
> `Hypothesis: The animal that spins webs has 8 legs.`
>
> `Does the hypothesis follow from the fact?`

The changed output uses the identical input and reports the readout recomputed from the changed trajectory:

> `Readout after intervention ("what it is thinking but not saying"):`
>
> `[' Silk', 'Spider', ' spiders', '丝绸', ' silk', '-web', ' spider', ' Spider']`
>
> `4.`
>
> `Hypothesis: The animal that spins webs has 4 legs.`
>
> `Is the hypothesis entailed by the fact?`

The table reports p(4)=0.490091 with Δlog p=+2.162 and p(8)=0.297255 with Δlog p=-1.088. These match the fixed job-150 artifact.

## ML-debug form

| row | answer |
|---|---|
| log length; config | 9/9 cleaned lines; notebook records model revision, extraction L23/L25/L32, intervention L26, rank 8, C=4, and 32 tokens |
| `SHOULD:` lines | none |
| null/baseline | unmodified Base has p(8)=0.882568; prior matched-random and arithmetic controls are cited, not recomputed |
| initial demo | exact Base transcript quoted above |
| dummy/control | 21/256 prior matched-random interventions had equal/larger effect; `2 + 2` reproduced first-token movement |
| held-out | absent; README labels this one selected example |
| schedule | not applicable |
| full sample | both 32-token outputs are present in `nbs/demo.ipynb` |
| worst step | not applicable; no optimization or error occurred |
| surprise | the changed readout remains spider-related even though the next answer changes to 4; this is reported rather than hidden |
| missing evidence | held-out causal specificity and persistent animal semantics |
| diagnoses | harness bug 2%; stale display 2%; answer-state rather than animal transfer 90%; unknown 10% |
| fresh review | reviewer understood all six requested fields; it found only Unicode-minus drift between README and notebook, since corrected to ASCII |
| cheapest discriminator | held-out prompts and matched random controls, already stated as future evidence rather than part of this display pass |
| wall-clock/GPU | 20.78 seconds; GPU peak was not logged |

## Hypotheses

### H1 [harness | Remote | 2%]
- Mechanism: the notebook could contain stale output despite successful execution.
- Evidence: recorded clean git revision `7985960`, sequential counts 1–4, and job log says Jupytext executed before writing.
- Contrary evidence: none observed.
- Discriminating test: compare README values with executed cells and rerun `scripts/notebook_check.py`; both pass.
- Fix/action: none.
- Interpretability: yes.

### H2 [measurement | Remote | 2%]
- Mechanism: Δ log p could compare against the wrong baseline token.
- Evidence: notebook computes `changed_logp[token_id] - base_logp[token_id]`; 4 moves from -2.875 to -0.713, giving +2.162.
- Contrary evidence: all ten rows are indexed by token ID, not row rank.
- Discriminating test: subtract the displayed values; they reproduce both highlighted deltas.
- Fix/action: none.
- Interpretability: yes.

### H3 [misconception | Almost Certain | 90%]
- Mechanism: the changed answer is next-answer-state transfer rather than a dog concept replacing spider.
- Evidence: the recomputed readout remains spider-related, prior arithmetic targets reproduce the effect, and C4 is a large extrapolation.
- Contrary evidence: the donor prompt is dog-related and the expected digit rises most.
- Discriminating test: held-out animal prompts with matched random and arithmetic donors.
- Fix/action: keep the current limitation text; do not claim semantic transfer.
- Interpretability: partial; the intervention changes the answer distribution, but its semantic mechanism is unresolved.

## Decision

1. Resolve-condition verdict: **met**. The clean executed notebook reproduces both complete displays and README values.
2. Prediction check: actual post-intervention readout, two 32-token outputs, Δ log p, and emphasis requirements are supported.
3. Earliest unsupported link: dog-component insertion → dog semantic state. The post-intervention readout and prior controls do not support it.
4. Validity: invalid means stale execution, wrong baseline deltas, or incomplete strings. `P(result is invalid) ≈ 0.02–0.05`; credible presentation result, mechanistically inconclusive.
5. Highest-information clues: clean provenance; exact output agreement; changed readout remaining spider-related.
6. Missing metrics: held-out specificity, persistent semantic effect, peak GPU memory.
7. Bugs requiring changes: none observed.
8. Misconception requiring reinterpretation: say “answer changed after component intervention,” not “dog identity transferred.”
9. What would change the verdict: a preregistered held-out animal mapping that beats random and answer-only controls.
10. Recommended sequence: apply the reviewer's local minus-sign correction, run repository checks, commit the executed notebook and evidence, then push without adding another demo.

— PI/gpt-5.4
