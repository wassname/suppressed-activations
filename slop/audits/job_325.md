# Job 325 audit

Target: chat-formatted, four-position L23 replacement with a delayed readout.

Provenance: pueue job 325, `/workspace/2026/suppressed-activations`, command
`uv run scripts/delayed_readout.py --output-dir out/2026-09-06_224500_delayed-chat-readout`,
started 2026-09-06 17:39:29 +0800, ended 10 seconds later, exit 1. The full five-line cleaned
log and raw pueue log agree. The queued code was commit `dc06f50` plus an unrelated README edit;
the job did not record its revision itself.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| Python imports | load shared hook helpers | import stops before model load | no | `ModuleNotFoundError: No module named 'scripts'` | no model state | no scientific result |
| model and chat template | load Qwen and render inputs | absent | no | no lines after traceback | rendered prompt | no format check |
| intervention/readout | write `result.json` and `run.md` | absent | no | output directory absent | detector rows | resolve condition not judgeable |

## Primary evidence

`pueue log 325 --full` is the primary execution record. It contains:

> Installed 57 packages in 272ms
> Traceback (most recent call last):
>   File "/workspace/2026/suppressed-activations/scripts/delayed_readout.py", line 19, in <module>
>     from scripts.demo import generate, intervention_hooks, layer_hooks, top_tokens, trajectory
> ModuleNotFoundError: No module named 'scripts'

Epistemic context: pueue's direct stdout/stderr capture for this job. The exception occurs before
`AutoTokenizer.from_pretrained`, so it establishes only the execution-path failure.

The failure is almost certainly the script execution import root: Python puts the script directory,
not the project root, at the front of `sys.path`. This is consistent with the command being
`uv run scripts/delayed_readout.py` and with the absolute failing import.

## Hypotheses

### H1 [bug | Almost Certain | 95%]

- Mechanism: the entry script imports `scripts.demo` without adding the repository root to
  `sys.path`.
- Evidence: the complete job log quotes the failing import and module name above.
- Contrary evidence: none measured; the same helpers import successfully as `python -m scripts.test`.
- Discriminating test: run the entry script after adding its parent directory to `sys.path`; H1
  predicts model loading begins, while another import problem reproduces before it.
- Fix/action: add the project root to `sys.path` in the entry script, then run its existing unit
  test and restart job 325.
- Interpretability: no. The job produced no model output.

### H2 [harness | Unlikely | 20%]

- Mechanism: pueue changed the working directory despite `-w`.
- Evidence: the raw log records `Path: /workspace/2026/suppressed-activations`.
- Contrary evidence: the recorded path matches the requested worktree.
- Discriminating test: print `Path.cwd()` at script startup after H1's fix.
- Fix/action: no change unless the startup path differs.
- Interpretability: no.

## Decision

- Resolve-condition verdict: not judgeable. The label required a chat-formatted delayed readout;
  the import exception preceded all measurements.
- Prediction check: the plan predicted that all detector layers would follow the patch. Unresolved:
  no forward pass ran.
- Earliest unsupported link: importing the experiment entry point.
- Validity: invalid, `P(result is invalid) ≈ 0.95–0.99`; the only result is an import error.
- Missing metrics, ranked: rendered chat inputs; condition readouts; final-token probabilities.
- Recommended sequence: fix H1, run the CPU unit test, restart job 325, then inspect the full
  `run.md` and `result.json` before changing the public notebook.

-- Codex/gpt-5
