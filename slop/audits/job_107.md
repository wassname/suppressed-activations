# Job 107 audit

Job 107 was the same-protocol retry of job 106 through `.local/run_demo_final.sh`. It ran the dog demo and figure from clean commit `fb140c0` and exited 0. `data/causal_demo.json` records `v0.1.1-16-gfb140c0` without a dirty suffix. The 8→4 result, 248/256 random rank, prompt-only generations, and dose response reproduce jobs 90–104. The redrawn PNG has no missing Thai glyph; `just demo-check` subsequently passed after stale README and `<think>` assertions were corrected. Sources: [`job_107_full.log`](job_107_full.log), [`job_107_status.json`](job_107_status.json), [`final_demo_check.log`](final_demo_check.log).

Validity: valid for the selected spider→dog example, about 0.95–0.99. It remains exploratory because dog and C=2 were selected in earlier runs. No new scientific discrepancy appeared.

Written by PI/gpt-5.4.
