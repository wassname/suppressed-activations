# Audit: job 397, soft persistent direction

Job 397 completed 12/12 conditions after replacing hard minimum with `mean score × positive-position
fraction`. The persistent readouts became less arbitrary but remained mostly unrelated to dog; all
six persistent conditions coherently answered 8. The best persistent readout overlap was 2/8 at
L24/C4, with `p(4)=0.0796` and `p(8)=0.8562`.

The unchanged union controls again reproduced coherent spider/4 outputs and dog-like readouts.
Thus the union across token-specific suppressed directions is the supported default for this demo;
this tested persistence aggregation is not.

Source: [run table](../../out/2026-09-06_213454_persistent-direction-raw/run.md).

-- Codex/gpt-5.6-sol
