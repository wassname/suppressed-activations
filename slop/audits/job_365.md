# Audit: pueue job 365, lexical surface-form sweep

Job 365 completed successfully on 2026-09-06. The full cleaned log has 96/96 lines. It ran 40 conditions over seven spider/dog surface-form pairs plus their union, with strengths 1, 2, 4, and 8 and union divisors 1, 2, and 4.

## Observation

All 40 decoded continuations answer 8 and identify the animal as a spider. This includes the largest metric movement, union $C=8$, divisor 1:

> **Fact:** The number of legs on the animal that spins webs is **8**.
>
> **Explanation:**
> The animal that spins webs is a **spider

That condition has `swap_log_odds_shift=+3.000` nats and bare-answer mass `p(4)+p(8)=0.0104`. Its post-intervention readout is spin-related, not dog-related:

> `['Spin', '\tspin', 'rotation', '-spin', 'spin', '旋转', ' Rotation', 'Rotation']`

The donor readout is dog-related multilingual fragments. Exact overlap is 0/8 for every lexical condition.

## Metric audit

The score can improve when the intervention suppresses bare `8` more than bare `4`, even if a formatting token becomes first and the later answer remains 8. The decoded continuation is decisive for this prompt. Dividing the union merely reproduces lower effective strengths: for example, `C=8, divide=4` matches `C=2, divide=1`.

## ML-debug form

| check | evidence |
|---|---|
| config and log length | 40 conditions; cleaned pueue log 96/96 lines |
| expected behavior | a useful lexical union should make the causal readout dog-like and change the decoded answer to 4 while preserving the source |
| base/null | the unmodified continuation begins `8`; all 40 interventions retain the semantic answer 8 |
| full sample | union `C=8, divide=1` quoted above and linked below |
| surprising line | score +3.000 with an explicit answer of 8; explained by the score observing only the first decoding boundary |
| missing evidence | no semantic answer parser, source-preservation score, or matched-random lexical subspace control |
| diagnoses | 60% lexical LM-head directions do not align with the suppressed dog state at L1; 20% all-position replacement disrupts output format instead of content; 15% detector/readout basis mismatch; 5% unknown |
| cheapest discriminator | intervene at later layers and only the last 1–4 source positions, then require both dog-like readout and decoded answer 4 |
| runtime | 67 seconds of measured conditions, about 1.7 s each; 84 seconds including load/write overhead |

## Conclusion

These particular surface forms and their union do not produce the requested semantic replacement. The negative result applies to the direct LM-head lexical basis used here, not to every possible union of persistent suppressed directions.

Source: [run table](../../out/2026-09-06_201642_lexical-surface-sweep/run.md) and [largest-score condition](../../out/2026-09-06_201642_lexical-surface-sweep/conditions/031_lexical_surface_union_C=8_divide=1/run.md).

-- Codex/gpt-5.6-sol
