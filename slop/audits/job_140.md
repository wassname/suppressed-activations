# Audit: pueue job 140

Target: job 140, `uv run scripts/spider_ant_demo.py`, run from `/workspace/2026/suppressed-activations` at clean source `v0.1.1-45-gc587b8e`. It failed after 21.4 seconds with exit 1.

| stage | expected | observed | expected? | clues | missing metric | consequence |
|---|---|---|---|---|---|---|
| provenance | clean source and resolved configuration | source `v0.1.1-45-gc587b8e`; metadata records Qwen3.5-4B, L26, rank 16, C=8 | yes | `out/2026-09-05_200002_spider-ant/metadata.json` | dependency versions | source is recoverable; exact package state is only in that commit's lockfile |
| model load | load Qwen3.5-4B in BF16 | 426/426 tensors loaded | yes | `slop/audits/job_140_full.log:2-5` | peak GPU memory | failure occurred after model load |
| extraction | construct prompt-specific rank-16 suppressed subspace | execution reached Dog calibration | unclear | traceback reaches `evaluate_prompt` line 278 | selected vocabulary rows | no extraction result was persisted |
| equal-distance calibration | match Ant, Dog, and Bird perturbation norms | BF16 residual and FP32 directions reached one `einsum` | no | `slop/audits/job_140_full.log:14-26` | all target distances | no target comparison exists |
| qualitative generation | 64-token Ant, Dog, and Bird generations | not reached | no | no `result.json` | all generations | no behavior can be interpreted |
| quantitative evaluation | test 6/4/2 and 32 random controls | not reached | no | no result table | every outcome metric | resolve condition is not judgeable |
| persistence | complete `metadata.json`, `result.json`, and `log.md` | partial metadata and placeholder log remain `running`; no result | no | `out/2026-09-05_200002_spider-ant/` | terminal failure state | partial directory must be read with this audit |

## Chronology and primary evidence

The queue label stated the question and decision condition:

> `why: Spider-to-Ant changed 8-to-6 but random controls can do the same; resolve: at equal residual distance test whether Ant/Dog/Bird map to 6/4/2`

Source: `slop/audits/job_140_status.json`.

The model loaded successfully:

> `Loading weights: 100%|██████████| 426/426 ...`

Source: `slop/audits/job_140_full.log:4-5`. The full clean log is 26/26 lines (`pqlog 140 100000`).

The first animal calibration then failed inside the coordinate calculation:

> `candidate = coordinate_swap(clean_h, animal_directions, middle, mask)`
>
> `return torch.einsum("bsd,sqd->bsq", h, torch.linalg.pinv(directions))`
>
> `RuntimeError: expected scalar type BFloat16 but found Float`

Source: `slop/audits/job_140_full.log:14-26`. The model was loaded with `dtype=torch.bfloat16`, while `projected_pair` derives directions from `.float()` LM-head rows. The failed source passed the captured BF16 residual directly into this FP32 coordinate operation.

The same error reproduces without the model:

> `RuntimeError: expected scalar type BFloat16 but found Float`
>
> `exit_status=1`

Source: `slop/audits/job_140_reproduction.log:1-10`. The minimal repair is to cast the captured residual to FP32, matching the existing intervention hooks.

The artifact stopped before results:

> `"status": "running"`

Source: `out/2026-09-05_200002_spider-ant/metadata.json`. The directory has no `result.json`, so it contains no scientific observation.

## ml-debug form

| row | answer |
|---|---|
| log length; config | 26/26 clean lines. Metadata: Qwen3.5-4B, prompts spider and byte, extraction L23/L25/L32, intervention L26, rank 16, C=8, 32 random controls, 64 generated tokens. |
| each `SHOULD:` line | None was emitted. The preregistered prediction was Ant/Dog/Bird → 6/4/2 at equal distance. It remains untested. |
| null for each cited number | No scientific number was produced. Exit 1 is compared with required exit 0. The dtype reproduction also exits 1. |
| initial demo | Not persisted. The base behavior was not reached in the report path. |
| dummy/control | Random controls were not reached. |
| baseline and held-out | Neither was persisted. |
| schedule | Not applicable; no training. |
| one full sample | Input recorded as `Fact: The number of legs on the animal that spins webs is `. No model output exists. |
| worst step | Calibration crashed before a metric. No loss or gradients exist. |
| surprising lines | The run loaded the full model before a dtype mismatch detectable with two small tensors. Explained by absent dtype preflight. |
| missing to trust | All target distances, logits, generations, random controls, completed metadata, and result JSON. |
| diagnoses | H1 dtype bug 99%; H2 unbracketed distance calibration 35% as next failure; H3 other runtime bug 20%; unknown 10%. |
| fresh review | Reviewer: “The minimal repair ... `.float()` ... is correct” and “The equal-distance search is not bracketed.” See `slop/reviews/20260905_job140_crash_review.md`. |
| cheapest separator | FP32/device assertions plus one no-op coordinate swap. A C=64 endpoint-distance check separates reachable from unattainable calibration. |
| time and memory | 21.4 seconds. Peak GPU memory was not persisted. A CPU tensor preflight would fail before model load. |

## Hypotheses

### H1 [bug | Almost Certain | 99%]

- Mechanism: BF16 captured residuals and FP32 projected directions entered one `einsum`.
- Evidence: `RuntimeError: expected scalar type BFloat16 but found Float` appears in both `slop/audits/job_140_full.log` and the minimal reproduction.
- Contrary evidence: none; PyTorch requires matching operand dtypes for this operation.
- Discriminating test: cast `clean_h` to FP32 and rerun the minimal coordinate call. Success supports this diagnosis; the same error would contradict it.
- Fix/action: use `residuals[INTERVENTION_LAYER][None].float()` and assert dtype/device equality.
- Interpretability: no scientific result; the software diagnosis is interpretable.

### H2 [method | Unlikely | 35%]

- Mechanism: Dog or Bird may not attain the Ant perturbation distance by C=64, so unbracketed bisection would converge to the endpoint and fail the `1e-3` equality check.
- Evidence: the reviewed code fixed the upper endpoint at 64 without checking reachability; see `slop/reviews/20260905_job140_crash_review.md`.
- Contrary evidence: no distance was computed before the dtype crash, and coordinate-swap distance may be large enough.
- Discriminating test: evaluate distance at C=64 before bisection. A distance below Ant's proves the requested match unattainable in the interval.
- Fix/action: fail with the measured endpoint and target distances before bisection.
- Interpretability: partial; a clean failure would delimit the intervention, but still not test semantics.

### H3 [harness | Highly Unlikely | 20%]

- Mechanism: another animal-target or report path may contain a runtime bug because this new branch has not completed once.
- Evidence: job 140 is the first execution of this path and persisted no result.
- Contrary evidence: `python -m py_compile scripts/spider_ant_demo.py` and `uv run python -m scripts.test` pass after the repair.
- Discriminating test: rerun the complete script and require complete metadata, `result.json`, every generation, and the final result table.
- Fix/action: audit the rerun rather than adding silent fallback behavior.
- Interpretability: no for job 140; a complete rerun can be interpreted.

## Decision

1. Resolve-condition verdict: **not judgeable**. The requested “test whether Ant/Dog/Bird map to 6/4/2” did not reach any logits or generations.
2. Prediction check: target-animal semantics, prompt-specific digit perturbation, and calibration bug are all **unresolved** because no outcome metric was persisted.
3. Earliest unsupported link: equal-distance calibration. Persisted target distances would support it.
4. Validity: invalid means the run cannot answer its queue-label question. `P(result is invalid) ≈ 0.99`; classification: **invalid run**, not a negative scientific result.
5. Highest-information clues: (1) exact dtype exception, because it localizes the crash; (2) identical minimal reproduction, because it removes model behavior; (3) absent `result.json`, because it prevents accidental interpretation.
6. Missing metrics by information gain: calibrated distance per target; expected-digit logits and 32 matched random effects; complete 64-token generations and post-intervention readouts; peak GPU memory.
7. Bugs requiring code changes: H1 requires FP32 captured residuals and dtype/device assertions. H2 requires endpoint reachability validation.
8. Misconceptions requiring reinterpretation: none; no scientific conclusion was produced.
9. What would change the verdict: a complete rerun with equal achieved distances and 6/4/2 target-specific outcomes would support semantic steering; shared or unrelated digits would support generic prompt perturbation.
10. Recommended sequence: run static checks, commit the minimal dtype and bracketing repair with this audit, then restart the same queue job. Do not alter rank, layer, strength target, or prompts in the rerun because combined changes would remove attribution.

Written by PI/gpt-5.4.
