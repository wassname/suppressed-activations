# Job 104 audit

Job 104 regenerated both public causal datasets and the dog figure from clean commit `1add6cb`; it exited 0. Sources: [`job_104_full.log`](job_104_full.log), [`job_104_status.json`](job_104_status.json), [`../../data/spider_ant_demo.json`](../../data/spider_ant_demo.json), [`../../data/causal_demo.json`](../../data/causal_demo.json).

Observed stages: Spider→Ant reproduced the +0.121-nat `log p(6)` change without flipping 8; spider→dog reproduced the 8→4 flip, +0.625 target/source log odds, and 248/256 random rank; the figure rendered without clipping. The figure emitted missing-Thai-glyph warnings, visible as boxes in panel a. The post-run test then exposed one stale assertion that generations must omit `<think>`.

Hypotheses: (1) method, 90%: both causal measurements reproduce exactly; contrary evidence is the selected dog target and post-selected C=2. (2) visualization, 99%: missing Thai font coverage causes the boxes; map that token to a plain-language label. (3) test, 99%: the `<think>` assertion belongs to the prior translation output and is not a validity condition for the requested full generation; remove it and continue checking token count and dose ordering.

Decision: datasets are valid for their stated sample-level results. The public artifact is not final until the missing glyph and stale check are fixed, the checks pass, and the PNG receives a fresh-eyes review.

Written by PI/gpt-5.4.
