# Audit: job 391, decode-persistent chat intervention

Job 391 completed 32/32 conditions. The full cleaned log was read. The official chat template was
used identically for extraction and generation, with thinking disabled and no added instruction.

Every continuation still identifies a spider and answers 8. Repeating the edit during cached
decoding at C≤2 did not transfer the answer. The strongest four-layer conditions changed
`arachnids` to `arachids`, showing that the hook was active before obvious corruption, but did not
change the answer. The causal readout reached 4/8 donor overlap in the prompt prefill.

This rejects the specific hypothesis that a small repeated dog-component replacement would keep
the chat answer state active until the later digit. It does not reject persistent directions:
the operation projects each generated token into a prompt-derived source/target basis, which need
not represent the same state during decoding.

Source: [run table](../../out/2026-09-06_212901_persistent-generation-chat-fact/run.md).

-- Codex/gpt-5.6-sol
