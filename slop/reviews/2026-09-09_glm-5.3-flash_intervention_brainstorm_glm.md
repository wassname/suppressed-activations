---
requested_model: z-ai/glm-5.3-flash
max_tokens: 4000
final_tokens: 20000
reasoning_effort: low
input_bytes: 42632
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_intervention_brainstorm_glm.trace.jsonl
generated: 2026-09-09T07:51:51.830866+00:00
---

# bounded completion

## First call reasoning

We need to brainstorm hypotheses for why the current band interventions work on leg prompts but fail on naming prompts, and generally how a sample/token/layer-dependent suppressed subspace should be intervened on. Reconstruct evidence, propose mechanisms (implementation error, objective/gradient mismatch, unintended learning dynamic), with falsifiable predictions and cheapest checks. Observed vs inferred. No winner.

Key evidence to organize:

1. local-dog (L20 vs L25 selection peak, edit at L20, equal perturbation norm 18.94): peak L20 gives coherent dog naming; peak L25 gives "dog" then endless self-correction loops. Same basis family, same edit layer, equal norm. So selection peak changes the basis content (detector layers [18,20,32] vs [18,25,32]) even though edit layer fixed at 20. One selected span, projected mean-difference addition scaled to full-difference norm — not state replacement.

2. ant pair: frozen vs synchronized donor replacement at L24 C2, same first logits ("6") but frozen continues spider, synchronized continues ant. Synchrony = donor states recomputed from source-selected tokens each decode step.

3. Later band (L22/23/24, rank-4 C2): leg prompts work (dog=4, ant=6, description matches donor animal) but naming prompts answer Dog/Ant then correct back to spider. Also dog leg description factually incoherent ("two legs run, two support weight" — maintains identity, wrong facts).

4. Random-span controls: norm-matched random spans mostly stay spider lexically, but 3/8 dog and 1/8 ant random spans exceed selected span on the digit log-odds shift. So first-token logit shift is not specific; lexical continuation is (all random continuations spider). First-token mass ≠ semantic transfer.

5. Wrapper effect: proper user-message wrapper → clean clean/donor prose correct, but steered outputs stay spider. Bare-answer first-token mass not semantic.

6. Readout caveat: current pseudocode uses raw template attenuation selector (contrast-energy), not the original vocab rise-and-fall min(relu(peak-early),relu(peak-output)) detector. Lexical readouts unvalidated. Basis may differ.

Pseudocode: attenuation_basis computes J from left singular vectors of [peak; final] contrast columns, then E = (peakJᵀpeakJ − finalJᵀfinalJ)/n, top-r positive eigenvectors → "attenuated" directions (present at peak, gone by final). edit: project donor−source difference onto U, add C× that to source at the last-3 positions; at step>0 only position 0. Random condition: random orthonormal R, norm-matched per-position, using current random-condition state (not selected trajectory).

Notable observations to explain:
- Why naming fails but legs succeed. Naming prompts: source "Spider"/target "Dog" — capital, multilingual base output (蜘蛛 (Spider)). Dog naming intervention says 狗 (Dog) then "Wait, that is incorrect... spider." So identity leaks briefly then correction wins. Ant same.
- Frozen vs synchronized divergence at step>0: frozen uses donor states from donor's own generated tokens (which say ant/6) vs synchronized uses donor states for source-selected token. Actually pseudocode implements synchronized: donor_ids updated with source token each step, so donor states conditioned on same token content. Frozen presumably kept donor's own tokens.
- perturbation norms vary wildly per layer (22: 9.3, 23: 3.9, 24: 24.3 for band-dog). C=2 fixed. Layer 23 tiny norm — subspace nearly captures nothing? Or Δ nearly in U with small magnitude.
- Dog-fact incoherence: identity carried but property facts wrong → subspace carries "dog-ness"/name axis but not full property set, or the digit override conflicts.

Hypotheses:

H1 (implementation/selection mismatch): current attenuation_basis differs from original vocab detector; the recovered successes used detector-derived vocab directions; current basis is a different subspace (energy attenuation of template contrasts), so recovered behavior can't be expected to transfer. Prediction: recomputing basis with original vocab min(relu) detector at same layers/positions reproduces naming transfer (dog name persists ≥3 sentences) more often than current basis; cheapest check: rerun fixed-band-dog-name with original-selector basis (single GPU run, source hash shows code changed at g332 — actually fixed-* runs use current code that hashes True for suppressed_activation_subspace.py, i.e., current code produced them; the successful local runs were old code). Cheapest: recompute both bases, measure principal angles between them — CPU-only, no GPU. If angle distributions ~orthogonal, the selector change alone explains regression.

H2 (objective mismatch: basis fit on prefill-only 3 suffix positions from contrast pairs, applied at step>0 position 0 only, single token): basis tuned for answer-token positions, not sustained through generation; naming prompts have answer followed by reflexive correction because the model's world knowledge re-asserts at property-encoding positions. Prediction: extending edits to more decode positions or higher-rank/per-layer basis reduces correction-back frequency; check: ablate — apply edit at all decode steps vs only position 0, count correction tokens (cheap, few runs).

H3 (gradient/objective mismatch: mean-difference addition scaled to full difference norm overweights): local-dog 011's correction loop shows the intervention moved the token but left inconsistency detectable by the model — same first logit, conflicting downstream evidence. Predicts logit-lens trace: dog direction high at answer position, but property directions (legs, bark) still spider-like at subsequent positions. Check: logit-lens of "dog/spider" and "eight/four" logits across generated positions in the intervened run, no new generation needed for saved runs — but saved runs lack per-step logits; rerun once with logging.

H4 (synchrony is the load-bearing mechanism, not the subspace): ant frozen-vs-synchronized with identical first logits and identical perturbation norm shows downstream token conditioning matters as much as the basis. The band runs are synchronized; naming failure may reflect that for naming, knowledge reassertion in MLP layers 25+ dominates any rank-4 L22–24 patch. Prediction: frozen band variant will fail legs too; synchronized single-L24 with rank-8 full-difference (old ant recipe) applied to the naming prompt will still correct back — separating prompt difficulty from mechanism. Checks: two cheap runs; also compare naming under higher C or more layers.

H5 (prompt-condition interaction / wrapper): the wrapper experiment shows the same construction stays spider under proper chat wrapper, meaning the transfer is fragile to tokenization/context, not a robust subspace effect; naming prompts with "Answer:" prefill plus capitalized single-token answers engage different circuit (multilingual header, refusal/correction circuits). Prediction: correcting to bare-answer prompts with wrapper restores spider; check: run fixed-band-dog-name with the wrapper used in the wrapper experiment (1 run).

H6 (unintended learning dynamic analog — here, self-correction attractor): the recovered L25-peak run shows the model can be pushed to emit "dog" while its fact-checking circuit detects the contradiction and loops. The band naming runs show the same dynamic: first token steered, then verification overrides. This suggests the intervention shifts the answer head readout but not upstream evidence accumulation; the "suppressed subspace" only suppresses at the readout surface. Prediction: at correction onset, mid-layer (e.g., L28–32) residuals project strongly onto spider direction while early layers project dog; check: projection trajectory of the saved-generation re-run on dog/spider vocab directions per layer/position.

H7 (digit log-odds control reveals non-specificity): selected span's digit shift (4.375) is below 3 random spans, so the answer-token nudge is largely a norm/mass effect; semantic transfer comes from the continuation. So first-token metrics are uninformative; any evaluation keyed on first logits is measuring the wrong objective (gradient mismatch in the analysis, not the model). Check: report random-span lexical transfer rate vs logit shift correlation — should be ~zero from existing saved data (no GPU).

H8 (implementation error candidates in pseudocode):
- Random-condition norm uses current random-condition state's Δrandom but compares to the selected trajectory norm — norm trajectories diverge across conditions, so matched-norm claims don't hold across conditions; falsifiable: log per-step perturbation norms for both conditions; if they diverge >20% by step 5, matching is broken.
- Hook bound at ℓ−1 editing block output — check off-by-one: editing output of block ℓ-1 vs residual at input of ℓ; coverage says hooks at layer numbers; if the record counts differ from expectation (assert decode_hook_count == len(tokens)-1), prefill step hooks? At step 0 positions are last-three; assert counts. Falsifiable prediction: if hook layer is off by one, matched random at a "null" layer (edit at layer where basis explains ~0 variance, e.g., L23 tiny norm) should still change output — it doesn't seem to, but a direct check: hook at ℓ-1 vs ℓ, compare outputs; cheapest: one run toggling hook index.
- float32→dtype cast without norm restore: at L24 norm 24 is large; cast rounding is unlikely the issue but normalized-restoration toggle is one run.
- donor_states captured pre-hook of donor forward but donor forward has no hooks — fine.
- E computed as (peakJᵀpeakJ − finalJᵀfinalJ)/n over concatenated [peak;final] columns: this measures attenuation of contrast energy; but if contrasts at peak and final are nearly collinear (difference persists to final), E≈0 and the basis is noise-dominated. Prediction: for naming pairs (Dog/Spider unembedding-heavy), attenuation energy is lower than for leg pairs → smaller projected norm at answer positions; consistent with observed L23 norms ~2-4 vs L24 ~22-25. Check: compute E eigenvalues and participation ratio for leg vs naming templates offline — CPU, uses saved templates if logged, else one forward pass batch.

Observed vs inferred: outputs, configs, norms, hash mismatches = observed. Statements about circuits, suppression semantics, "regression" = inferred. The hash mismatch on all scripts for old runs means old code isn't current; we cannot claim current code reproduces old results — inferred risk.

Also note: local-dog used match_component_norm=true (scaled to full-difference norm) whereas band runs match_component_norm=false. So the recovered successes used component-norm matching; the band doesn't. That's a concrete formulation difference — hypothesis: norm matching matters. Falsifiable: run fixed-band-dog-name with match_component_norm=true; prediction correction-back disappears. Cheap.

Also rank 8 vs persistent_rank 4 / rank-four replacements. Old configs rank 8.

Layer selection: naming pairs use detector layers [20,22,32] — but the naming run edits at 22/23/24, and detector peak for name contrast may be different layer; local-dog showed peak layer L20 vs L25 changes outcome drastically. For naming, maybe peak is ~L25 and L22/24 band misses it. Prediction: single-layer sweep for the naming pair over 20–28 will find a layer with coherent naming; check: 9-run sweep, small.

Structure answer: (1) evidence reconstruction with observed/inferred split; (2) hypotheses grouped: selector/basis mismatch (objective), formulation differences (norm matching, rank, mean-difference vs replacement), synchrony mechanism, prompt/wrapper fragility, self-correction dynamic, implementation-error candidates (random-norm mismatch, off-by-one hooks, basis degeneracy); each with prediction + cheapest check; (3) cross-cutting cheap experiment battery; no winner.

Keep it reasonably concise but thorough. Use headers.
## First call answer

# Independent reconstruction and hypothesis grid

## 1. What is actually observed (vs. inferred)

**Observed (from records):**

- Old local-dog, both conditions: identical edit layer (L20), identical positions, identical perturbation norm (18.9395…), `match_component_norm: true`, `rank: 8`, mean-difference addition scaled to full-difference norm. Only the *detector peak* differs (L20 vs L25). Outputs diverge completely: coherent dog naming vs. "dog" + endless self-correction loop.
- Old ant pair: frozen vs. synchronized donor replacement at L24 C2, identical first logits ("6"), identical perturbation norm (24.8989…); frozen continues spider, synchronized continues ant. Synchrony conditions donor states on source-selected tokens.
- Band runs (L22/23/24, rank-4, `match_component_norm: false`): leg prompts transfer (dog→4, ant→6, descriptions follow donor). Naming prompts emit Dog/Ant for one token then correct back to spider. Dog leg description is factually incoherent (two running legs, two weight-bearing). Per-layer perturbation norms are wildly asymmetric (e.g., band-dog: 9.3 / 3.9 / 24.3).
- Random-span control: 3/8 dog and 1/8 ant random rows *exceed* the selected span on digit log-odds shift, yet every random continuation stays spider. So the selected span's advantage is not visible in the digit logit.
- Wrapper experiment: proper user-message wrapper gives correct clean/donor prose but both steered outputs stay spider.
- All script hashes are `False` for the old successful runs; only the fixed-* runs hash `True` against current source. The successful constructions were produced by code we do not have byte-identical.
- The pseudocode implements a **raw template contrast-energy attenuation basis**, explicitly not the original vocabulary rise-and-fall detector (`min(relu(peak−early), relu(peak−output))` on gain-weighted unembedding rows).

**Inferred (not observed):**

- That anything "regressed." The band runs differ from the successful runs in at least five formulation axes simultaneously: basis selector, rank (4 vs 8), norm matching (off vs on), edit construction (projection vs. state replacement), and prompt family. No single-axis comparison exists.
- That the subspace "suppresses" anything semantic. The digit log-odds control actively argues against specificity at the answer-token surface.
- Circuit-level claims about where identity vs. correction lives.

