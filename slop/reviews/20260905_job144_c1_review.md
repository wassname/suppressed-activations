# Job 144 fresh scientific review

## Observation

The C=1 probabilities and changes from clean are:

| target | expected | p(2), Δpp | p(4), Δpp | p(6), Δpp | p(8), Δpp |
|---|---:|---:|---:|---:|---:|
| Ant | 6 | 0.9281%, +0.2543 | 8.8058%, +3.1637 | 3.6708%, +1.0057 | 83.5472%, -4.7097 |
| Dog | 4 | 0.8138%, +0.1399 | 9.9141%, +4.2720 | 3.2186%, +0.5535 | 83.0096%, -5.2472 |
| Bird | 2 | 0.7414%, +0.0676 | 7.9710%, +2.3289 | 2.9324%, +0.2672 | 85.6964%, -2.5605 |

The preregistered row-wise diagonal criterion fails for Ant and Bird. Only Dog's expected digit gains more than the other target-associated digits. The largest increase is p(4) in every row. Under a column-wise reading, Ant gives the largest Δp(6) and Dog the largest Δp(4), but Bird does not give the largest Δp(2).

All three rows keep 8 top-1. The coordinate exchange is numerically correct to about 1e-6. Residual norm changes by about 1.9e-6. C=1 distances differ: Ant 3.9999, Dog 2.9664, Bird 2.8354. This was intended because C, not distance, was fixed, but distance may explain some cross-target size differences.

## Inference

The strongest alternate explanation is a shared redistribution away from 8 toward the clean runner-up 4, driven mainly by the common Spider-removal component and perturbation magnitude. The matched random controls from the prior run use the much larger C=8 Ant distance, so they do not scale these C=1 effects.

Earliest unsupported link: projected unembedding vectors have not been validated as independent animal-identity coordinates. The code verifies an exchange in its constructed coordinate system, not a Spider→target-animal state change.

Cheapest next test: decompose each C=1 edit into its source-removal and target-addition components. Score p(2), p(4), p(6), and p(8) in six forwards without generation. If source removal recreates the shared p(4) increase, it explains the current matrix. Target-specific additions would be the evidence for animal semantics.

Verdict: valid run; preregistered semantic criterion fails. The result favors shared digit perturbation.

— reviewer subagent, fresh context
