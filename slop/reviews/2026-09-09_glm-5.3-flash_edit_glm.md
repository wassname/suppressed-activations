---
requested_model: z-ai/glm-5.3-flash
max_tokens: 4000
final_tokens: 20000
reasoning_effort: low
input_bytes: 5674
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_edit_glm.trace.jsonl
generated: 2026-09-09T12:48:32.766715+00:00
---

# bounded completion

## First call reasoning

Propose mechanisms, falsifiable predictions, cheapest checks, one construction-level experiment (different from the L20 shared_replace one already proposed). Provide new experiment.
## First call answer

## Mechanism hypotheses

**M1 — Implementation error: fixed_delta norm scaling is applied to Δ, not to the live component.**
`match_component_norm` on Δ means 893/894 receive a Δ whose norm is fixed by the template contrast, regardless of how much live spider signal is actually present in h at each decode step. 894's loop could simply be a mis-scaled vector at the naming positions ([40,41,42] and decode pos 1) pushing h off-manifold, not a semantic failure of "ant" persistence.
- **Falsifiable prediction:** re-scaling Δ per-decode to match the component norm of the live h in the projector span (same direction) removes the Wait/loop in 894 without breaking 893.
- **Cheapest check:** log `‖component(h, U)‖` per decode step for 893/894 (one rerun with a hook, no behavior change); if the template norm is >2–3× the live component norm mid-decode, M1 is live.

**M2 — Objective mismatch: the edit target is the wrong state for the task.**
Frozen 908 replaces toward *donor-prompt* coordinates while the source has already advanced; synchronization (895) fixes the state but the ant still reverts to spider — i.e., the projector span at L24 contains the animal-identity direction only weakly, and once the replace shrinks (0.62 by end), the base model's spider prior takes over. The intervention is doing the right thing to a span that doesn't carry "ant-ness."
- **Falsifiable prediction:** probing h at L20 vs L24 in the attenuation span should show animal-identity decodability concentrated at L20 for this template pair; replacing at L24 therefore transfers too little identity per unit norm.
- **Cheapest check:** train a linear probe (ant vs spider vs dog) on cached residuals in the U-span across layers; no new generations needed if residuals are saved. If L20 span separates ant from spider far better than L24, M2 is live.

**M3 — Unintended dynamic: transient-replacement feedback loop.**
In 895 sync, the replace norm shrinks because donor and source histories converge on shared tokens — but the model's own sampled tokens feed back, and an early "蚂蚁" followed by low-norm steering lets sampling commit to the spider paragraph before identity locks. 908's persistent large replace avoids this by brute force, but toward stale coordinates, producing the loop. I.e., the failure is a race between autoregressive commitment and decaying steering, not the edit site.
- **Falsifiable prediction:** at C where the sync replace decays slower (higher C, same L24, same batch), 895-style ant should persist past the name; and a forced first-token sampling change (greedy vs current) should flip 894's loop on/off if commitment race is the cause.
- **Cheapest check:** recompute 894 under greedy decoding (no retraining, one short generation); a flip is cheap evidence, absence is weak falsification given M1 confound.

## Construction-level experiment (single, not a grid)

**Edit-site transplant at fixed machinery: run `shared_replace` with synchronized donor conditioning at L20**, using the *existing* L20 attenuation projector from 893/894 (the one that already yields coherent dog). One `--batch-spec`, four cells:

1. C=0 (identity control)
2. Reference cell: 893 dog L20 `shared_replace` synchronized, C equal to the working frozen C
3. 894 ant L20 `shared_replace` synchronized, same C
4. Same ant cell at C/2 (continuous-steering interior point, to confirm monotone dose response rather than a threshold artifact)

**Construction constraints honored:** source prompt unchanged; steering is continuous `h + C(donor_span − source_span)` every decode step; the common dog+ant rule applies (893 and 894 both steered in the same batch, dog is the reference cell, not a separate tuned condition); property/legs questions unselected; `--batch-spec` path verified against `synchronized-attenuation`'s existing spec format before launch (it must accept `layer: 20` with the local-c003 projector path — if the schema rejects L20 projectors, that itself is a finding and the experiment aborts rather than being silently coerced).

**Readouts:**
- Ant coherent persistence (ant paragraph, no Wait/loop, no spider reversion) → L24 was the failure site; the synchronized span at L20 carries ant-ness.
- Ant still reverts while dog stays coherent → span is not the axis; the add/subtract or conditioning mechanism (M1/M3) is implicated, since machinery and layer are now both "what worked for dog."
- Both break → the L20 projector span does not survive `replace` (source subtraction interacts badly with the fixed_delta-calibrated span), falsifying M2's premise at this site.

**Per-decode instrumentation mandatory:** store `applied_norm` per step for all steered cells (missing in 893/894). Do not compare first-step norms across cells as causal evidence — 908 vs 895 already showed identical first-step norms with divergent trajectories.

No winner declared among M1–M3; the experiment discriminates M2 (site) from M1/M3 (mechanism/dynamic) but not M1 from M3 from each other.

## Completion

- outcome: `complete`
- trace: `/workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_edit_glm.trace.jsonl`
