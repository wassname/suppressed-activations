---
requested_model: z-ai/glm-5.3-flash
max_tokens: 4000
final_tokens: 20000
reasoning_effort: low
input_bytes: 5581
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_post894_glm.trace.jsonl
generated: 2026-09-09T12:14:53.592465+00:00
---

# bounded completion

## First call reasoning

We need to brainstorm distinct mechanisms, falsifiable predictions, cheapest checks.
## First call answer

## Situation reconstruction

**Observed (from saved generations):**

- Attenuation-local c003 works at L20 on Q-style naming prompts for **dog** (893: `狗` + coherent 63-token dog paragraph, S_swap +24.94) and produces the **ant name first** but then an ant-correcting-itself loop (894). The same method produced 862 (dog, statement prompt) with identical token IDs.
- Synchronized-attenuation L24 (a *different* method) gives 863: `6.` then ant content — name token is a digit, not a donor-name token.
- Detector-c2 (jobs 888–890) moves nothing: steered ≡ clean Base spider, perturbation ~0.7–1.0 vs ~20–24 for attenuation-local, projected_norm_fraction 0.029. Runner/cache equivalence fully passes.
- Earlier L22–24 band runs: donor name first, then explicit correction back to spider; leg prompts did transfer body descriptions (dog/ant legs) — but matched-random sometimes beat S_swap, all spider bodies.

**Inferred:** (a) the local-c003 dog construction transfers across prompt families; (b) there are at least two independent failure modes — a wrapper/detector that fails to inject anything (suppression too strong or subspace wrong), and a coherence failure where the name is emitted but the continuation policy (web-spinner consistency, correction behavior) reasserts; (c) "first-token donor name" is necessary but not sufficient; coherent continuation is the actual target.

---

## Distinct mechanisms

### M1 — Implementation/scale bug in detector-c2 selector (under-steering, not wrong steering)
The ~30× smaller perturbation with projected_norm_fraction 0.029 suggests the shared rank-8 selector projects the steering direction almost entirely out of the live subspace. Falsifiable prediction: feeding the c2 vector at c003's *effective norm* at the same hook should move behavior (or the selector's output vector will have near-zero cosine with the c003 steering direction).
**Cheapest check:** offline (no GPU): compute cosine between detector-c2's output perturbation vector and the 862/893 c003 vector at L20. If cosine < 0.2, the two constructions steer different directions and c2 is not a valid wrapper test. One script, seconds.

### M2 — Objective/gradient mismatch: S_swap is optimized on the first token, but success requires name **and** body coherence
862/893 hit the name; 894 hits the name then the model's global "web-spinner question" prior overrides continuation. The steering objective (swap probability) is satisfied at token 1 while the model's consistency objective dominates tokens 2+. Prediction: steering strength / duration of continuous application correlates with loop length — longer/stronger application delays or removes the correction.
**Cheapest check:** rerun 894 with decode limited to 8 tokens vs 128 from *saved* artifacts, or simply score token-level logit margin of `蜘蛛` vs `蚂蚁` across positions 2–20 in the saved 894 log. If the spider logit overtakes `蚂蚁` by token ~3–5, the transient-name mechanism is confirmed without new inference.

### M3 — Unintended learning dynamic: self-correction attractor (RLHF-style "wait/incorrect" template) triggered by contradiction between steered name and steered/prompt semantics
"Wait, ants do not spin webs" is a canonical self-correction template; the model resolves name-vs-fact contradiction toward the correction register, then oscillates. Prediction: suppressing or steering the "Wait/correction" register token(s), or prompting without the fact statement ("What is this animal called?"), removes the loop.
**Cheapest check:** text-only edit of the prompt string (one tiny GPU run or reuse: does any saved generation with the bare-question form loop?). Prediction: r2 collapses, coherent ant paragraph.

### M4 — Method–animal mismatch: synchronized-attenuation L24 (863) is digit-head artifact, not naming
`6.` as first token indicates the L24 construction lands on a formatting/list head, not the animal-name subspace. Treating 863 as an "ant reference" (as 894 does implicitly) imports a wrong target. Prediction: the 863 first-token `6.` logit mass sits near bullet-number tokens; the c003-ant vector (894) has low cosine with the L24 sync vector.
**Cheapest check:** offline cosine between the two recovered ant vectors; and read the saved 863 logit for `6.` vs `蚂蚁` (both free).

### M5 — Prompt-family × construction interaction (content/token alignment)
893 works on both prompt families, so c003-dog is not prompt-fragile; but 894's loop is family-specific (statement prompt → fact contradiction). Prediction: a construction trained on the question prompt family (donor: coherent dog/ant answer to the *question*) removes the loop for both animals.
**Cheapest construction to try next:** **one rule, two donors, question-family prompts, local-c003, L20** — i.e., apply the validated c003 method with donor activations collected from coherent ant question-answers (not 863's L24 sync vector), steering continuously through generation, with a decode-time check that `蜘蛛` logit stays below `蚂蚁` for ≥20 tokens. This is the smallest single-axis change isolating M3/M5: it keeps method constant (c003), swaps only the donor-activation source.

### M6 — Isolation check: matched-random control for the next run
Given prior matched-random S_swap > selected cases, run matched-random attenuation vectors on the same question prompts as the M5 run. Prediction if M5 is real: selected vectors produce donor names, matched-random produce spider bodies or loop, with no digit-first tokens. Digit-first tokens in *any* steered run count as failure regardless of S_swap.

---

## Recommended next constructions (both cheap, serial, no new framework)

1. **c003-local, question-family ant donor** (M5): one steering rule, one animal, full continuations to 128 tokens; success = `蚂蚁` first **and** no correction-register token (`Wait`, `incorrect`, `wrong`) in the first 40 tokens.
2. **Same rule for dog** on the identical prompts (should replicate 893) — confirms "one rule, both animals."
3. **Matched-random control** on the same hooks/prompts.
4. Offline-only: cosine c2-vs-c003 steering vectors (M1 check) and 894 logit-trajectory read (M2 check) — no GPU.

No winner selected: M1 (selector projection), M3 (correction attractor), and M5 (donor-source mismatch) are all live; the offline checks disambiguate them before any further GPU spend.

## Completion

- outcome: `complete`
- trace: `/workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_post894_glm.trace.jsonl`
