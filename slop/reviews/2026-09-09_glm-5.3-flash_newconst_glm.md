---
requested_model: z-ai/glm-5.3-flash
max_tokens: 4000
final_tokens: 20000
reasoning_effort: low
input_bytes: 8968
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_newconst_glm.trace.jsonl
generated: 2026-09-09T12:54:32.491086+00:00
---

# bounded completion

## First call reasoning

Let me carefully parse this setup.

The user is running an interpretability/editing experiment on an LLM (apparently a Chinese-capable model, given 狗/蚂蚁/蜘蛛 tokens — likely GLM or Qwen). They have an activation steering/swap intervention ("suppressed activations") where:

- A **source** model run (spider context: question about naming a spider — the animal is a spider) gets its hidden states edited at some layer to replace spider associations with donor (dog or ant) associations.
- **local-c003** (`fixed_delta`): h' = h + C·Δ where Δ = U Uᵀ(μ_donor − μ_spider), scaled so ‖Δ‖ = ‖μ_donor − μ_spider‖ (rank-4 attenuation SVD projector). Applied every decode step (continuous steering at decode), plus prefill last 3 tokens. No live source subtraction. Same Δ every step.
- **shared_replace**: h' = h + C·(U Uᵀ d − U Uᵀ h) — subtracts the live source component in the projector span. Frozen variant: d = donor-prompt residual (stale, computed once). Sync variant: d = donor residual after donor prompt + source-selected tokens (live, donor forward pass re-run with source tokens appended).

Observed results:
- 893 local-c003 dog naming: persistent dog (狗 + dog paragraph to EOS) — success.
- 894 local-c003 ant naming: 蚂蚁 then Wait/incorrect loop to 128 — failure (contradictory, loops).
- 910 sync L20 C2 dog: 蜘蛛 + spider paraphrase (893 dog lost). Ant C2 matches 898 (spider throughout — wait, 898 was "immediate 蜘蛛, spider body" — hmm, that reads like sync L20 ant C2 produces spider, i.e., the edit fails to change the answer to ant? Actually wait, let me re-read: "Ant C2 matches 898" and in the other doc: "898 already ran ant C=2 at this construction (spider throughout)". So sync L20 ant C2 → spider output; the replacement fails for ant at L20 sync. Hmm, but earlier 895 sync L24 ant: "transient donor (蚂蚁 then restore spider paragraph)". So the ant edits generally fail to persist, and even the dog edit fails under sync L20.
- 911 frozen L20 C2 dog: 蜘蛛 + mash. Ant: 蜘蛛 + short spider paragraph. Both fail to name donor.

So: only 893 (local-c003, dog) persisted. Everything with shared_replace lost. The ant never persists in any construction.

Key differences:
1. **Projector**: local-c003 uses an attenuation SVD projector computed... both use "attenuation SVD span" — actually the table says local-c003's projector: attenuation SVD on template peak−output contrast, rank 4, direction estimator = matched_template_mean_difference, and template delta IS applied (fixed_delta). Frozen/sync: same attenuation SVD span, but direction estimator = template_attenuation_donor_coordinate_replacement, and template delta NOT applied (edit_measurement). So the projector rank/geometry may be the same span U, but the direction used differs: local uses μ_donor − μ_spider direction projected through U; frozen/sync use coordinate replacement in donor coordinates.

2. **Operation**: fixed_delta adds Δ (no subtraction of live spider span). shared_replace subtracts live spider span in U-span then adds donor span.

3. **Donor-history**: fixed_delta has no donor history (same Δ every step). Frozen has stale donor-prompt residual. Sync tracks live donor conditioned on source tokens.

4. Layer: 893/894 at L20; 895/908 at L24; 910/911 at L20.

Results summary:
- 893 (add, no subtract, static Δ, L20, dog): persistent donor. 
- 894 (same, ant): loops (蚂蚁 then Wait/incorrect loop) — contradictory.
- 910/911 (subtract, L20, both animals): spider names win.
- 895 (sync L24 ant): transient 蚂蚁 then spider restored.
- 908 (frozen L24): contradictory loops for both.

So "add-without-subtract" was hypothesized as why 894 loops (spider span remains, fights donor span) while dog succeeds by prior strength. The 910/911 experiments tested replace at L20 — both animals went spider, so dog persistence in 893 was lost; the doc concludes: neither frozen nor sync shared_replace at L20 keeps the dog name; donor-history alone doesn't explain 893→910 dog loss; "subtract-or-stale-target stays jointly implicated with add-vs-replace."

Now the request: brainstorm distinct mechanisms — implementation error, objective mismatch, unintended dynamic — each with falsifiable prediction and cheapest check. Then ONE genuinely new continuously-steered construction aimed at ant persistence while keeping dog. No winner chosen. No another layer or shared_replace frozen/sync cell. A prefill-only run may be named only as labeled diagnostic, never the candidate.

Compare explicit edit equations, projector/alignment, donor-history.

So I need to produce:

1. A set of mechanisms (three categories: implementation error, objective mismatch, unintended dynamic) with falsifiable predictions and cheapest checks.
2. One new construction, continuously steered, targeting ant persistence while preserving dog behavior.
3. Comparison of edit equations, projector/alignment, donor-history — probably woven into the analysis.
4. Constraints: source strings unchanged; continuous steering (i.e., decode-time hooks, not just prefill); common dog+ant defaults (run both animals in same batch?); C=0 identity in the same family/batch; --batch-spec if compatible; tiny CPU smoke on the real path; isolated runner; full continuations. No reserved strings (spinneret/legs/live-birth questions reserved; 888 naming strings and fixed-band already used as regression cases — can be used as regression, not eval). Also do not run another layer cell or shared_replace frozen/sync.

Let me think hard about mechanisms.

### Understanding the failure modes

894 ant local-c003: 蚂蚁 then Wait/incorrect loop to 128. The model names ant then reverts/loops with "Wait" and incorrect content — contradiction between the added donor span and the surviving live spider span. Because fixed_delta doesn't subtract the live source spider component, the spider span keeps re-asserting through the (autoregressive) decode; each step re-adds Δ but also keeps spider component h_U intact. For dog, prior/dog coherence wins; for ant, the ant is a more "spider-like" concept (small arthropod, insect vs spider confusion) so the residual spider span dominates and produces contradiction loops. Actually interesting: dog is far from spider semantically, so maybe dog's strong activation pattern overrides; ant is close to spider (both arthropods, six vs eight legs, small), so the spider span at the same positions competes within similar subspace — but U is rank-4 attenuation span...

Wait, actually let me reconsider: maybe the rank-4 attenuation projector U captures the "peak−output contrast" directions — the directions that discriminate template peak vs output. Hmm.

910/911: shared_replace at L20 both animals → spider. So subtract-and-replace with the attenuation projector actually removes the donor influence more effectively?? Under shared_replace, h' = h + C(d_U − h_U). At C=2, the replace should project h onto... if C=1, h' = h − h_U + d_U = h with its U-span replaced by donor's. At C=2, h' = h + 2(d_U − h_U) = h − 2h_U + 2d_U. That overshoots: if h_U is positive component, h' = h − h_U − (h_U − 2d_U)... it doubles the removal. Anyway.

Why would replace at L20 lose the dog where add-without-subtract kept it? Hypotheses:

- **Scaling mismatch**: fixed_delta normalizes Δ to full ‖μ_donor − μ_spider‖ norm (match_component_norm True on Δ), i.e., C scales the full template delta norm, which might be larger than the live component norms being subtracted in shared_replace. In shared_replace, match_component_norm False; the applied norm = C‖d_U − h_U‖ which depends on live states; if the live spider span h_U is small at decode steps (because generation has moved off-template), then applied norm is small — indeed 895 trace: applied_norm decays 31.9 → 1.2 median → 0.62 as histories converge. So sync's replace self-extinguishes: as donor conditions on source tokens, d_U → h_U, applied → 0. That's a self-terminating steering: it stops steering mid-generation, so the base model's spider prior reasserts. Frozen keeps larger replace (median 11.46) but toward stale donor-prompt coordinates which don't fit the source context → contradiction loops. Local fixed_delta keeps a constant Δ (pert 23.6 prefill; decode active positions each new token) — constant pressure, wins for dog, loops for ant.

So the core trade: **constant, context-independent pressure (local) vs self-annihilating or stale, context-dependent pressure (sync/frozen)**. The ant needs constant-ish pressure that's still semantically appropriate; the dog needs whatever it had.

Also possibly: **normalization asymmetry**. In fixed_delta, Δ = U Uᵀ(μ_d − μ_s) scaled to ‖μ_d − μ_s‖ (full norm, not the projected norm!). Wait: "Δ = U Uᵀ (μ_donor − μ_spider) then scaled so ‖Δ‖ = ‖μ_donor − μ_spider‖". So they take the projected delta then rescale to the FULL pre-projection norm. Since U is rank 4 (out of d_model ~ thousands), the projected delta has much smaller norm; rescaling to full norm amplifies the in-span direction by ~ sqrt(d/4)?? That's a massive amplification factor (e.g., ×30–×50 if d=4096 and rank 4: sqrt(4096/4)=32). That's a big deal: fixed_delta effectively applies a heavily amplified span direction. Meanwhile shared_replace applies component difference without such rescaling (match_component_norm False). So C=2 local ≈ huge effective gain on 4 directions; C=2 shared_replace = modest gain. That could be THE reason 893/894 look stronger. Implementation-error candidate: the "match_component_norm True on Δ" scaling inflates the fixed delta by the ratio of full-delta norm to projected-delta norm — an unintended gain boost, and it's the axis that actually differs from replace, more than add-vs-subtract. Cheapest check: recompute norms offline from saved intervention_record / template residuals (no GPU decode) — compare ‖Δ_applied‖ at C=1 to ‖d_U − h_U‖ at decode steps. Prediction: ‖Δ‖ ≫ typical replace step norms (e.g., ≥ ~5–10× median replace applied norm at same C and layer). Actually we have numbers: prefill perturbation_norm 23.61 (893) / 19.76 (894); replace first applied 8.63/6.08, median ~1.2 (sync). So indeed local applies much bigger vectors at prefill and per decode step. But is the amplification deliberate? "match_component_norm True on Δ, not on live h" — it matches Δ's norm to the full template delta norm. Hmm — that's arguably intended ("keep the full template effect") but it's a confound: add-vs-replace is entangled with gain. That's a good mechanism to name.

Wait, but note: the doc explicitly says 910/911 vs 893 changes two axes: subtraction AND donor-history; and we should not attribute to subtraction alone. Also gain. Actually three axes now: gain/normalization, subtraction, donor-history, projector-direction (estimator differs!). Note: the projector span may be the same attenuation SVD span U, but the *direction within the span* differs: local uses matched_template_mean_difference (μ_d − μ_s projected), frozen/sync use template_attenuation_donor_coordinate_replacement. Hmm, actually for shared_replace, the edit is defined by d_U − h_U per-step, which isn't a fixed direction. The projector U is the same span (attenuation SVD, rank 4) in both? The table says both use "attenuation SVD span" — local: "attenuation SVD on template peak−output contrast; persistent_rank 4. direction_estimator: matched_template_mean_difference." So U comes from attenuation SVD, then direction = mean difference. Frozen/sync: same span, estimator = donor_coordinate_replacement. So the geometry (span) same; the estimator affects what's computed as the edit.

### Mechanism brainstorm

Let me generate mechanisms across the three requested categories.

**Implementation errors:**

1. **Norm-amplification asymmetry (fixed_delta gain inflation).** Δ = U Uᵀ(μ_d−μ_s) rescaled to ‖μ_d−μ_s‖ (pre-projection norm). If only a small fraction of the template delta lies in the rank-4 span, rescaling multiplies the applied direction by ‖μ_d−μ_s‖/‖U Uᵀ(μ_d−μ_s)‖ ≫ 1. So "C=2 local" is not comparable to "C=2 replace"; 893's dog persistence may be a gain artifact, not add-vs-subtract. Falsifiable prediction: the ratio r = ‖μ_d−μ_s‖/‖U Uᵀ(μ_d−μ_s)‖ is large (≫2), so local-c003 effective per-step push at C=2 exceeds replace applied norms by ≥5× at the same layer; and if you clamp local Δ to the projected norm (no rescale), 893-style dog persistence degrades at C=2. Cheapest check: pure offline — load saved template residuals/projector from the 893/894 intervention records, compute r for dog and ant at L20. Zero decode cost. Even better: check if ‖Δ‖ for ant < dog (19.76 vs 23.61 prefill) correlates with failure.

Actually we have prefill perturbation norms: dog 23.61, ant 19.76 — ant's template delta is smaller, consistent with weaker push. Also the check "894 failed though same construction" suggests gain isn't everything — ant still failed. But the mechanism-level point stands for interpreting 893.

2. **Per-decode position targeting: active_positions=1 on the new token.** In local-c003 decode, edit only the newest token's residual at L20, every step, with the same Δ. The stale Δ was computed from template means (μ_donor − μ_spider) over naming-template activations — it's a "concept-mean" push applied at every new token regardless of the token's role. For ant, after 蚂蚁 is emitted, subsequent positions get pushed toward ant-mean even when the model is trying to write predicate content (webs? legs?), and the surviving spider span (never subtracted) keeps leaking spider predicates; the model then flags inconsistency → "Wait". Hmm, that's more of an unintended dynamic. Implementation-error variant: **prefill/decode asymmetry** — prefill edits last 3 tokens including position 42 but decode edits only position 0 of each new token; the edited decode residual at L20 for position t then propagates up through L21–L40 for that token, but earlier context tokens keep unedited spider residuals in KV cache below L20... wait — KV cache: the prefill-edited tokens were edited only at L20 prefill; their KV at layers >20 reflects the edited L20 output — actually residual edits at L20 propagate to all layers above via the residual stream, so keys/values at L21+ for prefill tokens are already shifted. OK that's fine.

Implementation error candidates more concretely:
- **C applied twice / off-by-one in position indexing** — unverifiable from given info; skip or frame as "audit the hook once": e.g., verify the decode hook edits the new token's L20 residual BEFORE layer 21 uses it, and that `active_positions=1` index isn't silently applying to a padded/wrong position in batched decode. Cheapest check: instrument one forward pass on CPU, dump h at L20 input/output for the new token, verify h'−h = C·Δ exactly, and confirm the next layer's input at that position differs.
- **`match_component_norm` scaling uses the donor−source template norm but the decode-time h is not the template mean** — i.e., mismatched reference for normalization (gain calibrated to templates, applied to live states). Overlaps with #1.
- **fixed_delta Δ never updated after prefill while prefill perturbs 3 tokens but decode only 1** — potential positional mismatch: Δ computed as a mean over template token positions is applied uniformly; position-averaging washes out position-specific structure (e.g., subject-position vs predicate-position components). For ant, the mid-generation tokens need a different direction than the naming position. That's more objective mismatch.

3. **Projector contamination / sign or ordering bug in the attenuation SVD for the ant pair**: If U is fit on template peak−output contrast of the *spider* run (source templates), the ant's (donor) mean difference may project into the span with a small component, so ant Δ mostly points off-span after U Uᵀ... no — U Uᵀ projects, so Δ is in-span by construction; but its norm ‖U Uᵀ(μ_ant − μ_spider)‖ could be small (ant too similar to spider in the span → cancellation), which then triggers the rescale to blow up residual noise directions in-span. So the ant edit could be amplifying **noise** directions: U Uᵀ(μ_ant − μ_spider) has small norm; rescaling to ‖μ_ant−μ_spider‖ multiplies by huge factor; whatever tiny in-span residue exists (maybe idiosyncratic template artifacts) gets amplified → noisy steering → loop. Falsifiable prediction: for the
## Final answer fallback reasoning

Produce: mechanism list (implementation error, objective mismatch, unintended dynamic) with predictions + cheapest checks; comparison of edit equations/projector/donor-history; then one new continuously steered construction for ant persistence keeping dog, no layers, no replace rerun.
## Final answer fallback answer

# Mechanisms

**M1 — Implementation error: fixed_delta scales Δ, not the live projection of h.**
local-c003 enforces ‖CΔ‖ = ‖μ_donor − μ_spider‖, but match_component_norm is on Δ, not on ‖U Uᵀ h‖. If the live source component already has component-norm > ‖Δ‖, adding Δ only partially overwrites the spider span; on prompts where the live span is smaller (dog), the same Δ dominates. This predicts the dog/ant asymmetry without any "prior strength" story.
*Falsifiable prediction:* per-token ‖U Uᵀ h‖ along the decode differs between 893 and 894; where ant loops (Wait tokens), the live spider span re-grows above ‖Δ‖.
*Cheapest check:* recompute U Uᵀ h on saved 894 hidden states (or rerun a 10-token CPU smoke logging component norms); no GPU job needed.

**M2 — Objective mismatch: "naming" is optimized, "persistence" is not.**
Δ is fit on template peak−output contrast (naming token). Nothing in the objective constrains the *body* attributes, so after the name is displaced, decode follows base-prior attributes — hence 894's `蚂蚁` then Wait-loop (name won once, no attractor for ant-body content), and 910/911's spider-name output (replace removed the name span and dog body was never represented).
*Prediction:* steering at the naming peak position gives high P(dog/ant token) at step 1 in all cells, but P(dog-attribute tokens) at steps 5–30 tracks the construction's donor-history only weakly.
*Cheapest check:* first-token logits from saved intervention records vs. attribute-token logprobs in continuations — pure offline analysis of existing JSONL.

**M3 — Unintended dynamic: frozen target drags decode toward stale coordinates (908's applied_norm stays ~11 to step 128).**
Frozen replaces toward donor-*prompt* residuals each step; as source drifts, the applied vector becomes an OOD constant push → mash/contradiction. Sync shrinks to 0.6–1.5 (histories converge) → base reasserts. So both replace variants lose dog because the *effective* steering magnitude decays (sync) or becomes misdirected (frozen).
*Prediction:* 911's last-applied (2.81) > 910's (1.47) yet 911 dog is a mash — i.e., persistence is non-monotonic in residual applied norm.
*Cheapest check:* correlation of per-step applied_norm with dog-token logprob in 910/911 records, offline.

# Equation / projector / donor-history comparison

- **Edit equations:** fixed_delta adds a constant (no subtraction, no history); shared_replace subtracts the live span but its *target* is either stale (frozen) or converging (sync). Both replace variants lack a persistent drive; fixed_delta lacks correction. Persistence needs a nonzero, correctly-aimed drive at every step — fixed_delta has magnitude but stale aim, sync has aim but vanishing magnitude.
- **Projector:** all three share the same attenuation-SVD span (persistent_rank 4), so the span is controlled; failures are in the *time-profile* of the coefficient along the span, not the span itself.
- **Donor-history:** the only construction with live donor conditioning (sync) converges to the source by construction (donor prompt + source tokens), which *guarantees* decay. Donor-history conditioning as implemented actively defeats persistence.

# New construction (candidate): **error-driven replenishing delta at L20** — "fixed_delta with live-span servo"

Keep the 893/894 L20 attenuation projector and the add-only form (which is the only construction that ever held a dog to EOS), but make Δ a closed-loop signal computed *inside the hook each decode step* from the current source state — no second forward pass, no donor history:

```
per decode step t, at L20, on the new token position:
  s_t = U Uᵀ h_t                     # live source component
  Δ_t = U Uᵀ (μ_donor − μ_spider)    # same fixed template delta as 893/894
  g_t  = clamp( ‖U Uᵀ Δ_t‖ − ‖s_t − proj_U pre_edit_ref‖ , 0, ∞ )   # residual gap
  h'_t = h_t + C · (g_t / ‖U Uᵀ Δ_t‖) · Δ_t
```

I.e., **add-only, but the coefficient C·g_t is a servo on the live source span**: when the spider span has decayed to donor level, drive → 0 (no drift toward mash, unlike frozen); when the spider span re-grows (894's Wait-loop regime), drive replenishes (unlike sync, whose drive collapses by construction). This changes exactly one axis relative to 893/894 — the time-profile of the scalar multiplier — leaving projector, edit form (add), and source strings fixed. Dog should stay (893's constant drive was already sufficient; servo only reduces it as the span saturates); ant gets replenishment where 894 lapsed.

- Continuous steering: yes, applied at every decode position via the same hook, same as local-c003.
- C=0: g-term multiplies C, so C=0 is exact identity, same family.
- Prediction (falsifiable): applied_norm should start near 894's first-step value, decay as ant-span approaches donor level, and *re-spike* exactly at the steps where 894 emitted Wait tokens. If applied_norm decays monotonically and the ant still loops, the servo hypothesis dies.
- Cheapest pre-check: replay 894's saved decode hidden states offline, compute g_t per step from the recorded trace; if g_t never re-opens (no span re-growth), M1/M3 are wrong and the candidate should be shelved before any GPU run.
- Cell spec: L20 C0/C2, dog+ant, naming, one `--batch-spec`, tiny CPU smoke on the real runner path, isolated runner, full continuations. No prefill-only candidate run (prefill-only permitted only as a labelled diagnostic if needed).

Not claimed as winner; it competes with M1's diagnosis (which would suggest simply rescaling by live-span norm — a cheaper variant of the same servo).

## Completion

- outcome: `completed_after_follow_up`
- trace: `/workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_newconst_glm.trace.jsonl`
