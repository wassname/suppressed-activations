---
requested_model: z-ai/glm-5.3-flash
max_tokens: 4000
final_tokens: 20000
reasoning_effort: low
input_bytes: 4682
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_propbind_glm.trace.jsonl
generated: 2026-09-09T14:57:36.656189+00:00
---

# bounded completion

## First call reasoning

We need to explain: prose identity transfers to target, but yes/no head stays source-bound (spider). Answer position IS patched. Mechanisms: (1) answer head reads pre-answer context differently—binary readout uses a compressed/different feature subspace not spanned by U (rank-4 projector misses the yes/no decision direction); (2) reflection overshoot at C=2 breaks answer (prop-ant flips Yes→No at C=2, degenerate logits) — implementation/dynamics mismatch; (3) objective mismatch: Δ fitted on prose templates, so gradient mismatch for binary classification head; (4) learning dynamic: residual stream at answer position computed mostly pre-attentively... Let me structure ~4 mechanisms with predictions and cheap checks, observed vs inferred.

Key observation 6: p_source and p_target ~0 for the yes/no at answer position across all cells — degenerate logits. That means the binary prompt's answer position behavior is dominated by something else; observed `Yes`/`No` may come from weak signal. prop-ant transferred at C=1 then reverts at C=2 — consistent with reflection: P h' = 2Δ − P h; at C=1 full replacement gives target; at C=2 overshoot pushes back toward... reflection about Δ. Overshoot in the decision direction flips. Prediction: prop-ant Yes returns at C≈1.2–1.5 and flips again the other way / degrades at C=3.

Mechanisms:

M1 (subspace mismatch / gradient mismatch): U (rank 4) fit on prose templates captures identity/prose features; yes/no head reads a different direction. Δ ∉ decision subspace → answer head effectively sees source residual component outside U unchanged; it keys off the unpatched property-keying directions that still say "spider" (source prompt unchanged). Prediction: rank-increasing U or adding decision-direction template (answer tokens Yes/No in fitting templates) restores target answer at C=1 without hurting prose. Cheapest check: ablate—project answer-position residual at content_end onto U vs complement; compute cosine of (h_yes − h_no mean diff) with U span; or simpler: logit-lens / patch test with a delta built only from Yes/No answer-token means. Cheapest: measure ‖P h‖ fraction of norm at content_end and check whether yes/no direction lies in span(U) via cosine.

M2 (implementation/reflection overshoot): span_corrected at C=2 is reflection 2Δ − P h; sign flips along coordinates where |P h| large. Prop-ant Yes→No flip exactly at C=2, mirror eight-legs stays source `Yes`, and legs digit overshoot (4→2) all fit overshoot. Prediction: monotonic in C: yes/no correctness peaked at C=1, degraded symmetrically at C=0.5 and C=2; random control (matched norm) at C=2 destroys prose text but answer follows similar reflection pattern. Cheapest check: rerun prop-ant at C ∈ {0.75, 1.25, 1.5} — already have 1.5 = Yes; predict flip threshold between 1.5 and 2.0.

M3 (unintended learning dynamic: answer head reads a summary token position earlier / attention shortcut): the binary head's `Yes/No` may be determined by attention to earlier tokens (the property phrase "spins webs" / "eight legs") which are themselves source-bound via the prefill patch coverage only last-3 — the property name tokens (positions <41) are unpatched, so they carry source semantics that the head attends to, while prose generation reads the patched end. Wait—source prompt is unchanged, and hooks only patch prefill last-3 + decode last-1; earlier prompt tokens (naming the animal, property) are never edited at all in any mechanism; the injected Δ must propagate via attention. Prose head reads the patched site directly (adjacent); the yes/no classification may be done by a head attending to the property phrase or the animal name (earlier, unpatched tokens) — a different readout path. Prediction: patching only the animal-name token (or a mid-prompt position) transfers the answer without patching the last-3; conversely adding the answer direction at last-3 only won't. Cheapest check: log attention of the answer position at decode step 1: if top-K attention mass is on early prompt tokens rather than positions 41–43, readout path is elsewhere.

M4 (prompt-format / label-prior mechanism): the binary prompt ("Answer Yes or No:") has a strong label prior and the degenerate p_source≈p_target≈0 suggests calibration collapse — the model outputs from a near-flat or prior-dominated distribution; the observed Yes/No flips at different C are noise-scale artifacts, not binding. Prediction: bootstrap/rerun with temperature 0 vs sampling shows answer instability; response at C=1 ant Yes is fragile. Cheapest: rerun prop cells with 5 seeds / greedy verify; check logit values (not just probs of source/target tokens) at answer position — full next-token distribution at content_end.

Also note: mirror case eight-legs C=2 gives Yes (source) while prop-ant C=2 gives No — not uniform "source-bound"; it's reflection geometry: source Yes is pushed away from Δ... Under reflection both should reflect identically; the asymmetry (ant Yes→No, eight-legs stays Yes) suggests direction-dependent geometry, supporting M1/M3 over pure C=2 story. Mention.

Observed vs inferred tagging. Write concisely.
## First call answer

# Reconstruction

**Observed:** identity/digit prose transfers cleanly at C=1 (obs. 1), overshoots at C=2 (obs. 2, 5); the binary answer head is not uniformly source-bound — prop-ant transfers at C=1 and *reverts* at C=2 (obs. 4), while mirror eight-legs stays source-`Yes` at C=2 (obs. 5); answer-position logits are degenerate in both source and target directions at every C (obs. 6); the answer position **is** inside the patched window and the patch fires (obs. 7).

**Inferred:** "source-bound yes/no" is not one phenomenon. It decomposes into (a) a C-dependent reflection artifact, (b) a subspace/gradient mismatch between Δ and the binary readout, and (c) possibly a different attention readout path for Yes/No than for prose. The degenerate logits (obs. 6) mean whatever produces the emitted Yes/No is *not* the p_source/p_target channels being logged — that alone invalidates the simple "head sees source delta" story.

---

## M1 — Subspace mismatch: rank-4 U misses the yes/no decision direction (gradient mismatch)

Δ is fitted on **prose templates**; the Yes/No decision likely lives in a direction outside span(U). At the answer position, the unpatched component of h (which still encodes "spider context" from the unchanged source prompt) dominates the binary readout, while prose generation reads the patched end-of-prompt directly.

- **Falsifiable prediction:** cosine of the (h_Yes − h_No) template difference with span(U) ≈ 0; adding answer-token (Yes/No) means to the template set restores target answer at C=1 without disturbing prose transfer.
- **Cheapest check:** one forward pass at content_end; compute cosine(h_decision, P h) and ‖P h‖/‖h‖. If the decision direction is ~orthogonal to span(U), M1 is confirmed at near-zero cost. Also read the **full** next-token distribution at content_end (obs. 6 only logged two channels — the degeneracy may be an artifact of looking at the wrong logits).

## M2 — C=2 reflection artifact (implementation/dynamics)

P h′ = 2Δ − P h is a reflection. Overshoot along the decision direction flips binary answers; the digit 4→2 overshoot (obs. 2) is the same effect in numeric space. This predicts non-monotonicity: correct at C=1, wrong at C=2, and a threshold between.

- **Falsifiable prediction:** prop-ant is `Yes` at C ≈ 1.2–1.5, flips somewhere in (1.5, 2.0]; eight-legs flips target-ward near C=1 and back source-ward past some C*. Damage should be roughly symmetric about C=1 (C=0.5 ≈ C=1.5 quality).
- **Cheapest check:** already half-done — obs. 4 shows C=1.5 `Yes`, C=2.0 `No`. Run C ∈ {1.25, 1.75} on prop-ant. Cost: two decode runs.

**Caveat against pure-M2:** the mirror asymmetry (ant Yes→No vs eight-legs stays Yes at the same C, obs. 4 vs 5) means reflection geometry alone doesn't determine the flip — direction-dependent structure (M1) must be co-involved. Not discriminating; both stand.

## M3 — Different readout path: Yes/No head attends to early, never-patched tokens (unintended dynamic)

Hooks patch prefill last-3 + decode last-1 only. All earlier tokens (animal name, property phrase) are permanently source. Prose generation at the patched site can inherit target identity by local context; the binary head may classify by attending back to the property/name tokens instead — a shortcut path that bypasses the patch entirely. The naming results (obs. 8: digit 4 from a random in-span dog delta = generic quadruped prior) show prose readout is partly generic; the binary readout may be even more shortcut-y.

- **Falsifiable prediction:** attention mass from the answer position at decode step 1 concentrates on pre-41 tokens, not 41–43. Patching a mid-prompt animal-name token alone (leaving last-3 unpatched) moves the Yes/No more than patching last-3 alone.
- **Cheapest check:** dump attention weights of the first decode step for one property cell — one forward pass, zero retraining.

## M4 — Degenerate-logit calibration collapse (measurement/inference confound)

p_source ≈ p_target ≈ 0 at the answer position in every cell (obs. 6) suggests the emitted Yes/No comes from a near-flat or prior-dominated distribution, so the per-cell flips are fragile ordering artifacts, not stable "binding."

- **Falsifiable prediction:** greedy vs sampled answers disagree on some property cells; full-vocabulary top-5 at content_end shows yes/no tokens split across surface variants (` Yes`, `Yes`, newline variants) that the two-channel logging misses.
- **Cheapest check:** print top-8 tokens + logit values at content_end for prop-dog and prop-ant at C ∈ {1, 2}; compare greedy vs one sampled run.

---

## Observed vs inferred summary

| Claim | Status |
|---|---|
| Answer position inside patch window; patch fires | **Observed** (obs. 7) |
| Prose transfer at C=1, overshoot at C=2 | **Observed** |
| Yes/No head is a single mechanism reading the patched site | **Inferred, and likely false** — C-dependence and cell asymmetry contradict it |
| Reflection geometry (M2), subspace gap (M1), alternate attention path (M3), calibration artifact (M4) | All **inferred**; each has a ≤2-run discriminating check |

No winner declared. The single cheapest first move: dump the **full next-token distribution + attention row** at the content_end position for prop-dog and prop-ant at C ∈ {1, 2} — that one forward pass simultaneously tests M1 (which channels are live), M3 (where attention goes), and M4 (is the degeneracy real).

## Completion

- outcome: `complete`
- trace: `/workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_propbind_glm.trace.jsonl`
