---
requested_model: z-ai/glm-5.3-flash
max_tokens: 1500
final_tokens: 20000
reasoning_effort: low
input_bytes: 964
trace: /workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_span_pilot_glm.trace.jsonl
generated: 2026-09-09T14:11:59.504514+00:00
---

# bounded completion

## First call answer

## What the pseudocode does

**Setup:**
1. Compute the difference between two template means (μ_target − μ_spider) — i.e., the average direction difference between two classes of templates.
2. Project that difference onto a rank-4 orthonormal basis U (keeping only the component living in the U-span), then rescale it to have the same norm as the *original* (unprojected) difference. This gives Δ, the "template attenuation delta" confined to the U-subspace.
3. A random control is defined identically, except Δ is replaced by a random vector norm-matched the same way — presumably to test whether any direction-specific content matters versus just added magnitude.

**At generation time**, at each decode step and edited position:
- Take the live residual h and project it into the U-span (`span = U Uᵀ h`, i.e., P h).
- Replace the h's in-span component with C × Δ:
  - patched = h + C(Δ − span) means: subtract C of the current in-span content, add C × Δ.
- So the in-span part becomes an interpolation: C=0 leaves h untouched, C=1 replaces the span component entirely with Δ, C=2 extrapolates past Δ (reflecting P h through Δ).

The comment about "template attenuation" suggests the intent is to suppress/replace whatever the U-span encodes (a "spider-like" source component) with a "target"-like component — but the code itself only says *what* is computed, not what the semantics of U, spider, or target are.

## What is missing or underspecified

- **Where U comes from** — how it was learned/estimated, from what data, whether it's truly shared between the template statistics and the live residual stream.
- **Which layer(s) and which positions** are edited; "the edited position" is undefined.
- **What μ_spider and μ_target are averages over** (which prompts, how many examples, token positions).
- **Whether the norm-match uses the full difference norm or the projected norm** — the code scales by ‖μ_target − μ_spider‖ (full), which makes Δ generally *larger* than the projected difference; whether that's intended is unclear.
- **What model, task, or generation procedure surrounds this** — the loop body is only sketched.
- **Units/scale compatibility** — whether Δ's magnitude is appropriate relative to typical ‖span‖ at generation time.
- **What the random control's distribution is** ("random" — Gaussian? on what scale?).
- **Success criteria** — nothing says what the intervention is supposed to achieve or how it would be measured.

## Claims I would refuse to make from this alone

- That the U-span encodes anything specific (a "concept," "source style," or "spider-ness").
- That the intervention *causes* any particular change in output — no behavioral results are shown.
- That C=1 or C=2 has a meaningful interpretation beyond the stated arithmetic (e.g., "removal" vs. "amplification" in any causal sense).
- That the projection faithfully captures the template difference — the norm-matching step deliberately alters it.
- That the random control is an adequate baseline (e.g., that norm-matching controls for all confounds like residual norm interactions or outlier geometry).
- That the edit is localized — nothing here shows effects on other positions, layers, or tokens.
- That the intervention is monotonic, stable across steps, or that the "reflection" at C=2 behaves as the comment implies in practice.
- That this is a valid causal test of any hypothesis about the model — the code is only a mechanism, not evidence.

## Completion

- outcome: `complete`
- trace: `/workspace/2026/suppressed-activations/slop/reviews/2026-09-09_glm-5.3-flash_span_pilot_glm.trace.jsonl`
