# Round-2 revision

## Withdrawals and narrowings
- **Withdraw** one-shot / prefill-only as a final solution. Continuous every-decode steering stays mandatory; one-shot is diagnostic only. Conditional hooks are fine.
- **Withdraw** “prompt-contrast / layer-local attenuation” as an untried cure. Already tried; selected wins, weak transfer—status: prior art, not recommendation.
- **Withdraw** repeated-edit thrash as an established failure cause. Data also fit a poor axis; that claim is narrowed to a testable arm only.
- **Withdraw** any source-word blacklist or “mentions webs ⇒ fail.” `Dogs do not spin webs` is valid dog identity. Keep full-text checks for reversals, invented riddles, unrelated biology, repetition—not digit or word-counts.
- **Withdraw** rejecting the whole J-pair method from C2/L24 fails. C1 is exact exchange; C2 is extrapolation. Endpoint/layer rejects ≠ method death.
- **Narrow** “J-word plane is lexical tilt” from conclusion to **leading hypothesis**.
- **Correct dose formula:** for \(d=v_t-v_s\) and coefficient deficit \(a\), min-norm coefficient correction is \(a\cdot d/2\). \(a\cdot d/(d\cdot d)\) hits a **dot-product** margin—different units. Do not mix them. Pinv vs dot contrasts share sign (\(\times 1/(1-\rho)\)); not a gate-sign bug; neither is a validated semantic detector.

## Best continuous comparison (dose vs direction)
Fix layer and corpus. Hook **every** decode token (and last prefill). Same full-text evaluator as now.

| Arm | Direction | Dose |
|-----|-----------|------|
| A | J-pair \(d=v_t-v_s\) | C1 coefficient exchange |
| B | same \(d\) | continuous **coefficient-margin clamp** to clean-donor contrast: each step \(\Delta h=(a/2)d\) for deficit \(a\) (conditional gate optional) |
| C | matched-norm control (random unit dir in residual space, or \(d\) rotated orthogonal to \(\mathrm{span}(V)\)) | same coefficient-schedule energy as B |

Optional diagnostic: one-shot B once, then freeze—not a deliverable.

**Would establish:** B≫A on coherent dog/ant continuations ⇒ overshoot/dose hurt on this axis. B≈A, both fail, B≈C ⇒ direction/axis is the bottleneck, not dose. B>C but attribute paraphrases still fail ⇒ axis is weak/partial, not full identity.

**Would not establish:** true spider-concept causality, suppressed-span validity, transfer to arbitrary attributes, or that donor-margin clamp is a solved semantic edit—only whether failure is mainly bad dose vs bad direction under continuous steering.

## Bounded next step
Run A/B/C before new selectors or machinery. Donor-coefficient clamp remains a **candidate dose**, not validated concept replacement. Success = coherent counterfactual identity under full-text rules, not top-1 digit.