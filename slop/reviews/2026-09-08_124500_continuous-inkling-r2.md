Withdraw/narrow: (1) I do not reject continuous per-token editing—it is required; a one-shot is only a diagnostic control. (2) I withdraw any source-word blacklist; “Dogs do not spin webs” is compatible with dog identity, and the existing full-text evaluation already tracks reversals and fabricated biology. The counterfactual identity is not the error. (3) The coefficient gate is not a sign bug—signs match via (1−ρ)—but neither coefficient nor dot-product comparison is an independently validated semantic detector. (4) Correcting the math: for deficit a in coefficient space, min-norm correction is a·d/2; a·d/(d@d) targets a dot-product margin and mixes units. I should not have proposed the latter as coefficient-margin. (5) I narrow “repeated-edit dynamics cause failure” to “consistent with poor axis”; C1 is exact exchange, C2 extrapolation—not every swap is threefold overshoot, and rejecting one layer/endpoint does not kill the method. (6) Prompt-contrast/layer-local attenuation has been tried; it yields selected coherent examples but does not generalize across attributes, so I do not present it as an untried cure. More machinery waits on one bounded discriminator.

Best continuous comparison (separates dose from direction): At the layer where unprojected C1/C2 already move the digit (e.g., L12/L16), keep the same spider→target pair and active continuous hook on every decode token, but run four matched conditions: C0 (no edit), C1 (exact coefficient exchange), donor-margin coefficient clamp (apply a·d/2 to reach the clean-donor coefficient profile, never beyond), and C2 (extrapolation). Compare full continuations with the existing evaluation (reversals, invented riddles, entity consistency, repetition)—no digit-only or blacklisted-word scoring.

What it establishes: If only C2 fails and C1/donor-margin produce coherent donor-matched explanations, the problem is bad dose (overshoot/sign reversal). If all doses, including the donor-margin clamp, fail with the same retraction/reversion/biology errors, the axis is a bad direction at that layer, and continuous steering cannot salvage it regardless of dose. It does NOT validate the suppressed span, the gate, or generalization to other attributes; it only adjudicates dose versus direction for this pair/layer. The donor-margin clamp remains a candidate, not a validated solution.

Bounded discriminator (before further machinery): A paired, deterministic rubric comparing edited and clean-donor generations—check (a) digit-animal consistency, (b) absence of source-entity leakage and retraction language, (c) biological coherence of the explanation, (d) no repetition/riddle fabrication. Score success only if all hold; report failure mode categories. This is the required bounded check; do not expand to speculative selectors or new spans until this discriminator shows dose-sensitive separation.